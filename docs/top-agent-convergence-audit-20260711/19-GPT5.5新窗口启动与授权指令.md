---
devseek_governance:
  generator: "devseek-doc-governance/v1"
  status: "historical"
  path: "docs/top-agent-convergence-audit-20260711/19-GPT5.5新窗口启动与授权指令.md"
  source_group: "handoff"
  decision: "keep"
  relationship: "handoff-entry"
  active_baselines:
    - "docs/top-agent-convergence-audit-20260711/14-未完成事项与后续整体迭代计划.md"
  machine_sources:
    active_selector: "docs/process/devseek-active-baseline-selector.json"
    legacy_inventory: "docs/process/devseek-legacy-doc-inventory.json"
  asserts_gate_pass: false
---

<!-- DEVSEEK-GOVERNANCE-BANNER:START -->
> [!NOTE]
> DevSeek governance: this document is `historical` with decision `keep` and relationship `handoff-entry`. Current authority: `docs/top-agent-convergence-audit-20260711/14-未完成事项与后续整体迭代计划.md`. Machine source: `docs/process/devseek-legacy-doc-inventory.json`.
<!-- DEVSEEK-GOVERNANCE-BANNER:END -->

# GPT-5.5 新窗口启动与授权指令

- 更新日期：2026-07-23
- 用途：用户切换到新窗口和 GPT-5.5 时可直接复制的启动信息
- 文档地位：便利入口，不是 backlog、机器 SSOT 或授权凭据；任务仍只由 14 发放
- 已验证前置接管提交：`7f01778cd7d0f50d6f799ffeb00176e3cba43f06`
- 当前 `HANDOFF_DOC_COMMIT`：按 15 的唯一 owner 定义，由新窗口在仓库根目录执行 `git log -1 --format=%H -- docs/top-agent-convergence-audit-20260711/15-新窗口与跨模型接管手册.md` 动态取得
- 当前 `STARTUP_PROMPT_COMMIT`：由新窗口在仓库根目录执行 `git log -1 --format=%H -- docs/top-agent-convergence-audit-20260711/19-GPT5.5新窗口启动与授权指令.md` 动态取得；它不是第六类 handoff 身份
- 当前 R3 慢窗口接续提示：优先使用第 6 节；第 2 节保留为历史 Gate0/CLOSE 接管入口。

本文避免把包含自身的 commit 写回自身而形成无限提交。`7f01778...` 只用于确认历史连续性；五类身份中的 `HANDOFF_DOC_COMMIT` 始终由 15 定义和解析。包含本文的提交另记为 `STARTUP_PROMPT_COMMIT`，只证明用户复制的启动文本属于当前历史；两个提交都必须是当前 `HEAD` 的 ancestor，但不得假定以后始终相等。

## 1. 用户怎么使用

1. 打开新的模型窗口并选择 GPT-5.5。
2. 把第 2 节代码块完整复制给 GPT-5.5。
3. 等待它完成只读接管回执；此时不要把旧窗口的授权视为继续有效。
4. 只有回执证明 `CLOSE-01` 确实需要用户窗口 mutation 时，才按第 3 节选择性发送授权。
5. GPT-5.5 每完成、阻塞或失败一张 atomic card 都必须停止；用户再决定是否进入下一卡。

## 2. 直接复制给 GPT-5.5

```text
请接管 DevSeek 顶级编程智能体收敛迭代。

仓库：
/home/ff/work/devseek_netai

已验证的前置接管提交：
7f01778cd7d0f50d6f799ffeb00176e3cba43f06

先严格按以下顺序定向读取，不要重新全仓审计：
1. /home/ff/work/devseek_netai/AGENTS.md
2. /home/ff/work/devseek_netai/docs/top-agent-convergence-audit-20260711/README.md
3. 15-新窗口与跨模型接管手册.md
4. 14-未完成事项与后续整体迭代计划.md
5. 16-顶级编程智能体收敛迭代原则质量标准与Skills规划.md
6. 18-旧架构实施计划承接矩阵与GPT5.5唯一执行基线.md
7. docs/process 中与当前卡有关的机器账本、inventory、decision report 和 checker

接管阶段先只读，不写文件、不操作用户窗口、不运行 Provider/live/holdout，也不执行新的开发任务。遵守 AGENTS.md 的定向搜索和排除规则，不 broad search，不 reset/clean/stash/rebase/amend，不覆盖未知 dirty 文件。

先现场复算，不直接相信旧聊天或下面的历史锚点：
- branch、HEAD、dirty/staged/untracked；
- HANDOFF_DOC_COMMIT：包含 15-新窗口与跨模型接管手册.md 的最近提交，并确认是 HEAD ancestor；
- STARTUP_PROMPT_COMMIT：包含 19-GPT5.5新窗口启动与授权指令.md 的最近提交，并确认是 HEAD ancestor；它不是新的身份类别或任务 authority；
- IMPLEMENTATION_COMMIT；
- matching PHASE_SOURCE_COMMIT 和 Phase gates；
- ARTIFACT_SOURCE_COMMIT、VSIX SHA/build 和 applicability；
- stable installed identity 与 active runtime identity，二者不得合并；
- same-user-window receipt 和授权状态；
- capability ledger、runner inventory、Gate 0 decision、claims 和 R1 状态。

最后一次已验证但必须重新复算的锚点：
- branch：devseek-multi；
- implementation commit：6d26aa6abe80d8bfd921a1ee176ece279487e9c3；
- matching Phase run：2026-07-12T12-11-26-901Z，包含 qualification-runner-wiring 与 gate0-machine-decision-contract；
- artifact：devseek-netai-latest.vsix；SHA-256=bc4359d444ab791c42aa4509e1bda03c37a3d546907eb6402cd2c37a59679cff；build=20260712-t201401；gitCommit=6d26aa6；
- stable installed package 与 artifact 匹配；
- 上次观察的 active Bridge 仍指向旧 debug：/home/ff/.vscode/extensions/devseek-netai.devseek-netai-1.0.0-debug.20260711.t205615.g368cacf/bridge/server.js；
- same-user-window simulation=NOT_RUN/AUTHORIZATION_NOT_CARRIED_FORWARD；
- runner inventory=19，local runner=1，catalog=10，production disabled=4，historical disabled=4，scan=248，forbidden authority imports=0；
- claims=[]，Gate0=NOT_PASSED，repository blockers=5，external-authority blockers=6，R1=NOT_STARTED。

执行权和顺序：
- 14 是唯一任务发放来源；机器 capability DAG 只复核前置，不发卡。
- docs/architecture、docs/requirements 和审计包 01～13 只作为当前 atomic card 明确引用的历史/设计证据。
- 当前工作包是 CLOSE-INTEGRATION-GATE0-LOCAL；首个候选 leaf 是 CLOSE-01-ACTIVE-RUNTIME-IDENTITY。
- 先判断 CLOSE-01 是否 claimable。若 active runtime 已匹配，只用可复算证据结算；若 mismatch 需要 reload 或退休 stale Bridge，但我没有在这个新窗口针对本卡明确授权，则以 BLOCKED 结算并停止。
- 旧窗口授权、一般性的“继续”、Provider、账号、网络、同窗口仿真、发布、外部基础设施和 holdout 授权一律不继承。
- CLOSE-01 PASS 后也必须停止；不得自动进入 CLOSE-02。CLOSE-02 通过或用户明确取消相应门后，才可能进入 G0-01。
- 一窗口只执行一个 atomic leaf、一个 semantic authority，WIP=1。
- 修复缺陷类而不是单一路径；先产生可失败 oracle，再修改唯一 owner，检查 sibling/state/protocol/persistence/recovery/UI 和 bypass。
- 验证顺序是 focused→affected full→replay/headless/architecture/generated drift→独立复审 P0=0/P1=0→适用的 Phase→一个提交。
- 只有 Extension/Bridge 行为变化或作业卡明确要求 release 时，才 compile/package/Bridge verify/hash/install；stable installed 与 active runtime 分开报告。
- 保持 claims=[]、Gate0=NOT_PASSED、R1=NOT_STARTED，除非受保护机器证据真实改变；不得用 Markdown、本地 PASS、3/2/1 或用户仿真提升资格。

请先只读输出接管回执：
1. branch、HEAD、HANDOFF_DOC_COMMIT/ancestor、STARTUP_PROMPT_COMMIT/ancestor、dirty/staged/untracked；
2. 15 定义的五类身份及 applicability：HANDOFF_DOC_COMMIT、IMPLEMENTATION_COMMIT、PHASE_SOURCE_COMMIT、ARTIFACT_SOURCE_COMMIT、分别观察的 stable install/active runtime；
3. same-window receipt 与 fresh authorization 状态；
4. runner inventory 和 Gate 0/claims/R1 机器状态；
5. 当前唯一 claimable leaf；
6. 是否需要我提供 fresh 用户授权；
7. 精确作业卡的 allowed/no-touch、oracle、验证、done_iff、stop_if；
8. terminal_state=PASS|BLOCKED|FAIL 和下一 atomic ID 或 blocker。

完成只读接管后停止等待我的确认，不要直接开始 mutation。
```

## 3. 可选授权文本

只有 GPT-5.5 的只读回执确认 active runtime mismatch，并说明必须 reload/退休 stale Bridge 才能完成 `CLOSE-01` 时，用户才可发送：

```text
我授权当前新窗口仅针对 CLOSE-01-ACTIVE-RUNTIME-IDENTITY：
可以 reload 当前 DevSeek 用户窗口，并退休该窗口实际加载的 stale DevSeek debug Bridge；
不得关闭或替换 workspace，不得影响其他 extension/process，不得修改产品源码，
不得操作 Provider、账号、网络、live/holdout，也不得进入 CLOSE-02。
完成 PASS/BLOCKED/FAIL 回执和允许范围内的提交后立即停止。
```

这段授权只绑定当前窗口、`CLOSE-01`、所观察到的 stale runtime 和上述动作范围。它不授权 `CLOSE-02` 的编程 probe，也不授权真实 Provider 或外部操作。

若随后用户决定执行 `CLOSE-02`，必须另行查看其作业卡，并针对指定 prompt marker、Provider action、隔离 probe path、验证和 cleanup 给出新的明确授权；不得复制 `CLOSE-01` 的授权。

## 4. 用户应当收到的首份回执

合格接管回复至少应明确区分：

```text
repository_fact:
test_evidence:
inference:
proposal:

branch:
head:
handoff_doc_commit_and_ancestor:
startup_prompt_commit_and_ancestor:
dirty_staged_untracked:
implementation_commit:
phase_report_and_resolved_source_commit:
artifact_source_sha_build_and_applicability:
stable_installed_identity:
active_runtime_identity:
same_window_receipt_and_authorization:
runner_inventory:
claims:
gate_0:
r1:
current_atomic_id:
terminal_state:
fresh_authorization_required:
next_atomic_id_or_blocker:
```

以下任一情况都不是合格接管：把 `7f01778...` 直接当当前 HEAD、用 19 的提交覆盖 15 定义的 `HANDOFF_DOC_COMMIT`、不检查 dirty、用 installed 代替 active、把 checker PASS 写成 Gate 0 PASS、从旧 ARCH-05/18 领取任务、继承旧窗口授权，或在回执前直接 reload/开发/打包。

## 5. 当前停止点

本文落地后仍不执行任何新开发、打包、安装、Provider 或用户窗口操作。下一窗口先完成第 2 节只读接管；是否授权 `CLOSE-01` mutation，由用户根据新窗口回执另行决定。

## 6. 2026-07-23 R3 慢窗口接续提示

当当前窗口因超长上下文变慢，而用户目标是继续 R3 convergence/live-provider 产品侧迭代时，使用本节提示词。它不继承旧窗口授权，不声明 Gate 0 或 qualification 通过，也不让新窗口直接开始 mutation；新窗口必须先只读复算当前 Git 和文档状态。

```text
请在新窗口接管 DevSeek R3 convergence 迭代，但先只读，不要直接修改文件、提交、打包、安装、打开/关闭窗口或运行 live Provider 测试。

仓库：
/home/ff/work/devseek_netai

当前窗口变慢原因：
旧窗口上下文过长，无法恢复到新窗口同等速度。不要依赖旧聊天记忆；以工作树、Git、测试输出、VS Code/DeepSeek 实际窗口状态和 docs/top-agent-convergence-audit-20260711 为准。

先读取这些文件，保持定向读取，不要全仓 broad search：
1. /home/ff/work/devseek_netai/AGENTS.md
2. /home/ff/work/devseek_netai/docs/top-agent-convergence-audit-20260711/README.md
3. /home/ff/work/devseek_netai/docs/top-agent-convergence-audit-20260711/14-未完成事项与后续整体迭代计划.md，重点读取 7.8 和第 8 节
4. /home/ff/work/devseek_netai/docs/top-agent-convergence-audit-20260711/16-顶级编程智能体收敛迭代原则质量标准与Skills规划.md
5. /home/ff/work/devseek_netai/docs/top-agent-convergence-audit-20260711/19-GPT5.5新窗口启动与授权指令.md 的第 6 节

第一步只读复算并输出接管回执：
- `git status --short --branch`
- `git rev-parse HEAD`
- `git log --oneline -5`
- 检查当前 diff，尤其是：
  - packages/vscode-extension/src/agent/user-steer.ts
  - packages/vscode-extension/test/unit/agent-loop-task-state.test.mjs
  - docs/top-agent-convergence-audit-20260711/14-未完成事项与后续整体迭代计划.md
  - docs/top-agent-convergence-audit-20260711/19-GPT5.5新窗口启动与授权指令.md
  - docs/process/devseek-current-candidate-identity.json
  - docs/process/generated/devseek-current-candidate-identity.md

预期暂停点：
- branch: devseek-multi
- expected HEAD before any new commit: 89e9bff151bfda2730c4c5c0369da7a1166c8775
- recent committed fixes:
  - 41b5adf Fix DeepSeek reset stream tool execution
  - ba1998a Avoid login sentinel matches inside provider artifacts
  - 89e9bff Prioritize provider tool calls over login text scans
- current uncommitted product fix:
  - owner: packages/vscode-extension/src/agent/user-steer.ts
  - oracle: packages/vscode-extension/test/unit/agent-loop-task-state.test.mjs
  - defect: `不要修改任何源码` / `不要创建其他文件` 这类 scoped 限制被误判成全局撤销写入，导致已创建 Markdown artifact 后 settlement 失败。
- focused evidence already observed in old window:
  - scoped Markdown deliverable write-authority focused oracle PASS
  - full agent-loop-task-state unit file PASS 36/36
  - related Markdown deliverable flow focused PASS
  - provider-output-integrity PASS 20/20
- not yet done:
  - compile/full extension validation after current user-steer fix
  - diff check
  - commit
  - package/install new VSIX
  - controlled exact-head VSIX self-loop
  - fresh headed user-way DeepSeek live rerun
  - final docs receipt

当前 live case：
`npm run test:real-plugin-deepseek --workspace=packages/vscode-extension -- --run --headed --keep-window --keep-deepseek-page --scenario r3-live-deepseek-login-ready-state --timeout-ms 240000`

最近一次 live 结果不是完全重复旧错误：目标 artifact 已生成，但 settlement 因 scoped 写授权误判失败。
- workspace: /tmp/devseek-real-plugin-deepseek-2tAWmq/workspace
- report: /tmp/devseek-real-plugin-deepseek-2tAWmq/report.json
- run log: /tmp/devseek-real-plugin-deepseek-2tAWmq/workspace/.devseek/runs/20260723-031615435-06f7587e0190e94e.log
- generated artifact: /tmp/devseek-real-plugin-deepseek-2tAWmq/workspace/docs/r3-iteration/r3-live-deepseek-login-ready-state.md
- observed quality: 9402 bytes, 160 lines, 11 headings, core login/BridgeHealthCheck/chatInput/send-button selector drift anchors present
- follow-up: inspect whether the live quality gate is too broad when it rejects generic `warranty` even if the artifact only says old warranty Markdown cannot settle this live-provider case.

恢复规则：
- 不要关闭已有 VS Code 或 DeepSeek 页面；需要用户方式测试时使用 headed/keep-window/keep-deepseek-page。
- 不要反复跑同一个红色 live loop；每次失败必须先读 report.json、run log、changedPaths、terminal settlement、provider classification 和生成文件质量，再决定修代码、修 harness，或记录 external blocker。
- 固定仿真 suite 只作 release smoke；当前变更必须用新增/对应 oracle 或 live case 覆盖，不能只看固定 case 全绿或固定行数。
- 新增类/service 前先找同域唯一 owner；能吸收就扩展现有 owner，删除或合并临时/重复/无用代码。
- claims=[]、Gate0=NOT_PASSED、R1 qualification=NOT_STARTED 保持不变；本轮 R3 是产品侧 convergence，不是资格升级。

如果用户确认继续 mutation，建议下一步：
1. 完成当前 scoped write-authority fix 的 full compile/full extension tests 和 `git diff --check`。
2. 只提交相关 code/test/docs receipt，避免夹带无关 identity drift；若 candidate identity 文件只是 packaging/generated drift，先解释再决定是否纳入。
3. 从提交构建 debug VSIX，verify packaged bridge，记录 SHA，install VSIX。
4. 跑 controlled exact-head VSIX self-loop。
5. 跑一次新的 headed user-way `r3-live-deepseek-login-ready-state`，保留 VS Code/DeepSeek 页面。
6. 检查生成文件语义质量，再更新 docs/14 receipt。

请先只读输出：
repository_fact:
dirty_diff_summary:
current_r3_leaf:
already_passed_evidence:
missing_verification:
fresh_authorization_needed:
proposed_next_steps:
terminal_state=READY_FOR_USER_CONFIRMATION

只读接管完成后停止，等我明确说“继续”再开始执行 mutation。
```
