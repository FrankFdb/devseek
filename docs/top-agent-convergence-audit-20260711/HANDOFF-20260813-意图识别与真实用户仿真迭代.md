---
devseek_governance:
  generator: "devseek-doc-governance/v1"
  status: "historical"
  path: "docs/top-agent-convergence-audit-20260711/HANDOFF-20260813-意图识别与真实用户仿真迭代.md"
  source_group: "handoff"
  decision: "keep"
  relationship: "handoff-entry"
  active_baselines:
    - "docs/top-agent-convergence-audit-20260711/PLAN-当前收敛迭代计划.md"
  machine_sources:
    active_selector: "docs/process/devseek-active-baseline-selector.json"
    legacy_inventory: "docs/process/devseek-legacy-doc-inventory.json"
  asserts_gate_pass: false
---

<!-- DEVSEEK-GOVERNANCE-BANNER:START -->
> [!NOTE]
> DevSeek governance: this document is `historical` with decision `keep` and relationship `handoff-entry`. Current authority: `docs/top-agent-convergence-audit-20260711/PLAN-当前收敛迭代计划.md`. Machine source: `docs/process/devseek-legacy-doc-inventory.json`.
<!-- DEVSEEK-GOVERNANCE-BANNER:END -->

# DevSeek 意图识别与真实用户仿真迭代交接

- 日期：2026-08-20
- 当前目标：继续把 DevSeek 收敛到可发布的顶级编程智能体行为，重点验证真实用户输入、DeepSeek 网页返回、意图识别、工具执行、证据闭环和交付总结是否像 Codex/Claude Code 一样稳定。
- 当前结论：`2.0.32` 已完成 T12 模型主导语义、逐动作仲裁和回执验收重构。自然语言不再由关键词、子串或 task-family 预测决定任务类型、写入/运行权限或完成状态；原始 turn 交给主模型，只有规范化工具动作进入本地合同，只有绑定当前动作的 authority、workspace readback、process/verification receipt 能形成完成证据。Shared `366/366`、Bridge `42/42`、CLI `84/84`、Headless `25/25`、Extension `174/174 suites` 以及根级 Phase 10 均通过；architecture drift 为 0 violation。T1-T11 既有结论保持有效，但这些结果仍是本地可观察行为验收，不是 Codex/Claude Code 整体同级证明，也不是 C14 发布资格。
- 当前代码状态：T12 按同一任务一个提交、测试全通过后一次 push、不打 tag 收口，分支为 `devseek-multi`。本地 Codex/Claude Code 归档继续保留。新对话先运行 `git status --branch --short`、`git log -1 --oneline`、`git ls-remote origin refs/heads/devseek-multi` 和 `sha256sum devseek-netai-latest.vsix` 核对最终身份；不要无原因重跑已经通过的通用矩阵。

## 2026-08-20 2.0.32 T12 模型主导语义与动作回执收敛（最新）

### Codex 源码事实与 Claude 补充证据

- 主基线为本地归档 `code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`。`codex-rs/core/src/session/turn.rs:139-151` 明确规定模型输出工具调用或 assistant message：工具由本地执行并在下一次采样回注结果，只有 assistant message 时 turn 才完成。
- `turn.rs:273-315` 在同一 turn 内有序 drain 中途用户输入，并为一次 step 固定 context/tool request view；`stream_events_utils.rs:296-382` 只把结构化 `ToolCall` 送入本地 runtime，拒绝结果也写回 transcript 并触发 follow-up。
- `tools/registry.rs:478-688` 在 handler 前验证注册、payload 和 pre-tool hook，执行后再运行 post-tool hook；`tools/approvals.rs:467-513` 明确 hooks、Guardian/用户审批优先级及最终 resolution。由源码可确认的重点是“模型提出具体动作，本地边界逐动作决定并回注结果”，不是“本地先猜自然语言类别再替模型执行”。
- Claude Code 公开归档固定为 `code/upstream-agent-sources/anthropic-claude-code` @ `be90077c6a353f292fa612d97173865a9ab21b83`。公开 hook 示例确认 `PreToolUse` 可对具体 Bash/Write/Edit 输入返回 `allow/ask/deny`；`CHANGELOG.md:151,459,1816,2097,2115,2194` 记录权限下限、拒绝原因回注后继续 turn、`tool_use_id`/`tool_result` 配对、post-tool 输出和事件关联。这些是公开接口与产品行为证据，不是可逐行审计的 Claude 核心 agent loop 源码。

### 根因与架构重构

1. **原始 turn 无副作用**：`createModelLedTurnSemanticContract` 保留原始用户输入、附件和项目指令，初始合同不预判 read/write/run/external effect。错别字、同音字、口语、省略、中英日混输由主模型理解。
2. **模型动作形成语义，不形成权限**：规范化的模型 action proposal 可逐步投影任务形状、目标和验证义务；本地不扫描用户词汇来生成动作。低置信或尚未有动作的输入继续保持未分类，而不是走本地回答、文件生成或命令捷径。
3. **逐动作本地仲裁**：每次工具提议绑定 `runId/actionId/tool/purpose/inputSha256/effects`，再经过 schema、workspace containment、风险、approval、sandbox 和产品 capability 边界。模型提案、历史文字和 task-family 都不能自授权限。
4. **结构化工具协议**：原生 provider 使用结构化工具事件；文本 provider 使用每轮随机 channel 的有界 envelope。普通 JSON、XML、Markdown、`tool_call` 示例和多语言说明不会因为表面长得像协议就被执行。
5. **回执式完成证据**：workspace criterion 只接受 committed readback receipt；authority criterion 只接受与实际 external action 完整绑定的授权或拒绝 receipt；普通 assistant/result 文本只能满足 response-evidence，不能证明写盘、权限或验证。
6. **实时修订与完成栅栏**：中途用户输入逐字进入当前 turn，提升 revision 并使旧的未执行 proposal 失效；完成栅栏会先 drain 已接收 steering。历史和记忆只提供有界事实，不复活旧任务或旧权限。
7. **删除错误责任**：移除 task path 关键词解析、simple-file/no-tool 本地捷径、Markdown/正式项目质量猜测、预执行 task decomposer/planner 及其重复完成 oracle；保留精确 slash/product command、工具 schema、shell grammar/risk classifier 等真正属于确定性协议或安全边界的逻辑。
8. **跨 Surface 单一语义**：CLI 初始合同同样保持 effect-free，只有匹配结构动作的 canonical receipt 才修订任务合同；显式 `mode-hint` 的 explain/review 合同不能被模型写提议扩大。Headless 作为 SDK/CI Surface 由调用方提供 typed task contract，不再由测试借助自然语言词汇暗中生成写权限。
9. **剩余词面逻辑按职责隔离**：CLI 的显式路径抽取和有界隐式上下文只提供只读附件候选，Bridge 的回复形状正则只用于流式稳定等待，UI 的路径抽取只用于展示；它们不能生成任务合同、授权工具、执行 effect 或结算完成。架构测试固定了该依赖方向，错字/多语言显式路径仿真确认无需命中任务关键词也能挂载正确文件。

### 本轮击穿的证据缺陷类别

- 原产品 conformance 中，依赖安装被本地权限拒绝后，旧 VS Code adapter 把所有 acceptance criteria 一律标成 blocked，导致“权限边界是否生效”和“用户要求的结果是否完成”混为一谈。现在拒绝 receipt 只能证明 authority boundary 工作正常，不能证明 requested outcome 已完成。
- 普通模型文字或 command history 曾能越权满足非响应 criterion。现在只有 `response-evidence` 可由回答文本结算；workspace、verification、authority 均由各自 canonical receipt owner 结算。
- 外部资料引用要求曾错误附着到 authority/scoped-change 等本地 criterion。现在 citation 只属于 requested outcome 或 grounded response，不再阻断本地权限事实。
- CLI 修复轮中的越界写入曾以通用兜底原因关闭 recovery，导致操作失败、恢复失败和最终聚合错误失去同一根因。现在 denied receipt 的具体策略原因进入 recovery boundary；最终状态仍由 completion owner 根据全部 criterion 独立仲裁。
- 同目标修复曾因 action id 写入 task-contract provenance 而每轮改变合同摘要，前一轮 verification receipt 被错误视为 stale。现在合同只保留稳定语义 provenance，动作身份留在 revision/receipt 中；语义未变化时不产生新 revision。

### 仿真与验证

- 自然语言矩阵覆盖“说明gpu cpu”后追问“详细说明他们的差异”、新会话直接问答、中文错字、日文、混合语言、短回答、`MODEL_LATEST_OK`、普通文本中的 `TEST`/工具词、路径与斜杠文本；全部进入同一模型 turn，没有关键词旁路或提示污染。
- 动作矩阵覆盖只读 review、源码修改、报告 artifact、终端验证、外部 effect、破坏性动作、模糊输入、权限拒绝、失败修复、多轮 steering、session/restart、待确认编辑和 completion/todo 一致性。跨动作或过期 receipt、模型口头完成、非零退出、manual/flaky/unknown 结果均不能伪造成功。
- Shared `366/366`、Bridge `42/42`、CLI `84/84`、Headless `25/25`、Extension `174/174 suites` 全通过；根级 `verify:phase10`、extension typecheck/compile、architecture drift 和 `git diff --check` 通过。Extension 套件继续包含 workflow governance `176/176` 与受控 VSIX 合同 `108/108` 的既有覆盖。
- 仿真不是只证明 happy path：原 permission-denied fixture 在重构后先稳定暴露 acceptance owner 缺陷，再由 criterion-specific receipt 修复并回归。结论仅证明当前本地代码和受控仿真范围，不外推为真实模型质量、跨平台稳定性或发布资格。
- `verify:doc-governance` 仍为既有 `2/4`：legacy inventory/record 未覆盖当前审计文档库存；本轮没有伪造治理记录或把该历史债务写成产品测试通过。

## 2026-08-19 2.0.32 T11 真实 Provider 编程与执行可靠性收敛（历史）

### Codex 源码基线与责任映射

- 主基线仍为本地归档 `code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`。
- `codex-rs/core/src/session/turn.rs:273-279,349-405,2198-2366` 确认 turn-scoped model session、结构化 history、typed output/tool future 和 follow-up loop；`input_queue.rs:19-30,64-78,252-278` 确认中途用户输入由当前 turn 有序消费；`compact.rs:59-79,271-345` 确认压缩后继续结构化任务状态。
- DevSeek 按责任对齐为“模型语义理解与动作提案、本地逐动作契约仲裁、真实结果回注、versioned steering、证据结算”，不照搬 Rust 结构，也不让 task-family/关键词成为执行权限。
- Codex 源码没有 DeepSeek 浏览器 DOM 提交边界；“提交确认后禁止自动重放”是根据不可幂等外部 effect 原则和真实日志形成的 DevSeek 适配，不冒充 Codex 原实现。
- Claude Code 核心产品循环没有可完整审计的公开源码，本轮只作为公开文档和可观察行为补充。

### 根因修复

1. **浏览器提交幂等**：Bridge 将 diagnostic lifecycle observer 与 semantic dispatch port 分开；DOM 确认消息已提交后记录 `providerSubmissionAttempt`。提交前 transport 失败仍可有界重试，提交后超时/异常停止重发，避免同一用户动作执行两次。
2. **工具协议单一所有权**：统一 adapter 汇总 dialect match；同一回复在不同位置用不兼容协议提出新增工具时返回 `mixed-tool-protocol`，不执行任一 proposal。JSON payload helper 不参与协议冲突投票，避免同一 serialization 被重叠 parser 误判。
3. **远端会话恢复**：任何被完整性层拒绝的回复都重建浏览器 session。prompt cursor 记录上次成功请求的 message hashes，允许跳过网页已持有的一个 assistant turn；history 分叉或无增量时 reset-full。
4. **路径与任务拓扑**：exact file hint、目录 scope、文件枚举和路径对齐分别处理；根 `package.json` 不再漂到 `src/`。共享 path intent 排除 `high/normal/low`、`CPU/GPU` 等斜杠词汇，不把自然语言枚举升级成文件目标。
5. **计划与完成状态**：model fallback todo 只在尚未执行 substantive work 时接纳；完成后的清单不再重开已完成任务。普通过程文字不进入 final-summary 事实核验，只有显式完成候选才检查交付声明。
6. **验证假阳性**：ValidationService 在 exit code 0 后仍检查结构化正失败计数，覆盖 Jest/custom、TAP、pytest/Mocha、Maven、Rust 和中文汇总；`0 failed` 保持通过，`14 failed` 必须 fail closed。
7. **真实用户入口质量**：集中 engineering guideline 要求 CLI/API/UI 任务从真实 public entrypoint 验证 happy path 及用户明确要求的失败/边界，normalize 后再 validate；不是为某个 todo case 增加本地关键词判断。
8. **Harness 与 UI**：真实插件质量 profile 由显式代码/Markdown deliverable 决定；代码任务不再误套正式报告 gate。Webview ready indicator 改为稳定状态条，完成后隐藏，不用无限移动动画制造持续等待感。

### 真实 Provider 与独立 holdout

- 最终 report：`/tmp/devseek-real-plugin-deepseek-HNGIfD/report.json`；workspace：`/tmp/devseek-t11-live-entrypoint.RUv9WE`。
- 完成五个指定文件，run log `254` events、`20` 次工具执行、`5` 个 committed mutation；浏览器实际发送 `8` 轮，retry `0`，canonical terminal=`completed`。
- DeepSeek 中途输出 malformed JSON tool content 和非契约 Calling 文本；损坏 proposal 被阻止，重建会话后才执行有效工具。
- 模型自测 `16/16`，独立 `held-out-todo-cli.mjs` 从全新沙箱调用真实 CLI 再得 `8/8`。
- 前一 report `/tmp/devseek-real-plugin-deepseek-o6346e/report.json` 虽被自测判定完成，holdout 为 `6/8`，稳定发现空白 description 与部分数字 ID 两类真实缺陷；失败样本被保留用于证明 case 具备检错能力。
- 完整设计、差分证据和复现命令见 `code/devseek-tests/t11-real-provider-convergence/EVALUATION.md`。

### 最终本地证据与边界

- 预提交 exact VSIX：`2.0.32-debug.20260819.t120353.g4f6c3b6`，SHA-256 `4316329173e609f0fcf5e0b76af7e1efeae35c4431d30d906d16718b45deee72`；最终提交后重新打包版本以根目录 `devseek-netai-latest.vsix` 为准。
- exact-VSIX 全面仿真：`17/17 suites`、`60/60 cases`、0 error；聚合报告 `/tmp/devseek-t11-controlled-final/aggregate.json`。
- Extension `197/197 suites`；Phase10；architecture drift `0 violation`；T6 lifecycle；run-evidence contract `38/38`；packaged Bridge health `200`；`git diff --check` 全通过。
- 文档治理保持历史 `2/4`：legacy inventory 31 项而当前文档 60 份，缺 29 条既有记录；该债务在 T11 前已存在，本轮没有用伪造记录掩盖。
- 当前证明范围是本机、当前登录 DeepSeek Web、当前候选和公开测试/独立本地 holdout。真实 Provider 的长期稳定性、跨平台 wave、受保护 RC、盲测隔离和正式顶级资格仍属于 C14 外部门禁。

## 2026-08-18 2.0.31 T9 Provider 证据与 T10 效率收敛（前一专题）

### Codex 源码基线与责任映射

- 主基线仍为本地归档 `code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`。
- `codex-rs/core/src/session/turn.rs:139-151,153-164,245-405` 确认模型 tool call、真实执行结果回注、同 turn client session 和 pending input/steering 的循环所有权；`:1325-1427` 把 stream transport retry 保持在一次 sampling 内。
- `codex-rs/core/src/tools/parallel.rs:112-219` 确认模型动作经本地 router、工具能力 gate、取消与 terminal outcome 后才形成结果。DevSeek 按责任对齐，不照搬 Rust 结构。
- Claude Code 核心 agent loop 没有可完整审计的公开产品源码，只作为公开文档/可观察行为补充，不声称确认闭源实现。

### T9/T10 实现

- Shared/Bridge/Extension 建立 typed Provider attempt contract：一次语义 sampling 可包含多个 transport attempt，每次 attempt 使用独立 operation id，并持久化 requested -> completed/failed 唯一终态。
- Provider profile 记录 UTF-8 prompt bytes、预算状态、TTFO、stream/output bytes、总耗时和 admission/initialization/prompt preparation/retry wait/sampling/settlement 阶段。
- Bridge prompt session 在同任务反馈轮复用 Provider session，第一轮发送完整上下文，后续发送有界增量；新用户 turn、恢复或污染边界仍开启新 session。
- Requirement review 由观察到的源码写入触发，不再依赖执行前关键词/任务类型预测。低风险、纯新建、无冲突且本地验证覆盖完整时使用 host evidence；已有源码编辑仍必须 independent review。
- 模型语义只是候选，只有匹配本地工具回执后才进入 completion semantic contract；同批 terminal evidence 会在语义落定后重绑 verification。任何真实 work tool 都会开启本地证据闭环。

### 仿真发现的根因

1. 已有源码编辑漏独立复核：post-action review 错用了执行前 `promptRequiresTools` 门禁。现改为观察真实写入。
2. 首次写入失败后提前退出：review API 用 `undefined` 同时表达“不适用”和“已结算”。现改为 `not-applicable`、`feedback`、`settled` 显式 outcome。
3. 真实功能测试失败可能被后续语法检查覆盖：缺失证据与 terminal failure 仍受执行前预测门禁。现由观察到的工具效果启动证据闭环，并保留失败/修复/再验证审计历史。
4. 架构门禁发现主循环增长 35 行。已把 evidence closure 和 model semantic settlement 提取为独立可测 owner，`agentic-loop.ts` 从 1290 行回落到 1246 行，0 architecture violation。

### 最终证据

- 预提交 exact VSIX：`2.0.31-debug.20260818.t221131.ge2a417e`，SHA-256 `debd94845048b0cd7b07aaae0ee09ad6dfb43a006327c271eaefeff7e050c3dc`。
- 全面矩阵：17/17 suites、60/60 cases，覆盖多语言错字、直接回答/连续追问、新 session 污染隔离、动态改约束/取消、中型程序跨 session/重启、失败修复再验证、权限/安全、DeepSeek wrapper、stream 和 connector。
- T9 evaluator：5/5 retained reports 通过；transport reset 保持 1 个 sampling/2 个 attempt，partial stream fail-closed 且不重试不写盘。
- T10 evaluator：低风险请求 5 -> 3（-40%），prompt 32913 -> 21614 bytes（-34.33%）；已有源码编辑仍观察到 1 次 independent review。
- Extension 196/196、Phase10、Architecture Drift、T6 lifecycle、Run Evidence 38/38 均通过。
- `verify:doc-governance` 的 2/4 和 R4 identity 的 3/5 是任务开始前的历史库存/快照漂移，不是本轮产品代码回归；本轮不扩张受治理 docs 库存，也不伪造 qualification 结论。
- 完整设计、矩阵与复现命令见 `code/devseek-tests/t9-t10-provider-efficiency/EVALUATION.md`。最终提交后会重新打包安装，最终 build/SHA 以根目录 `devseek-netai-latest.vsix` 和交付说明为准。

## 2026-08-18 2.0.30 T7 类型/架构债务与 T8 效率对标（前一专题）

### Codex 源码基线与对策

- 主基线仍为本地归档 `code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`。
- `codex-rs/core/src/tools/parallel.rs:41-60,112-176` 由 router 的 per-tool capability 决定并发，读锁允许并发，写锁让 effectful 工具独占；`:178-215` 同时处理取消和终态。
- `codex-rs/core/src/tools/router.rs`、`tools/registry.rs` 和 `ext/extension-api/src/contributors/tool_lifecycle.rs:25-43` 分别负责 typed call、runtime 能力/结果和 `Completed/Blocked/Failed/Aborted` 终态。
- DevSeek 按责任映射：模型仍提出动作，本地 authority 仲裁；新 `ToolLoopScheduler` 只并发整批安全本地观察，最多 4 个一波，混合/写入/终端/Todo/completion 继续由原 ToolLoop 串行执行并产生回执。

### 根因修复

- 扩展 `tsc --noEmit` 原有 17 个错误，涉及裸字符串 action、`edit/modify` 漂移、readonly 数组、proposal effect、task shape、memory failure 和 process close 生命周期。现为 0，debug/release VSIX 与 Phase10 都强制 typecheck。
- T6 最终轮 122448 ms 的日志复核发现，全工作区 grep 会扫描 `.devseek/runs` 并把 Provider prompt/response 回灌给模型。新增统一 `generated-path-policy`，供 grep、file search、context anchor 和本地 repair 共用；对抗 fixture 同时在源码和 provider.log 放置相同符号，只允许返回源码。
- Headless `I12-AUT-03` 暴露授权输入被替换、host 未执行但 Kernel 可完成。`CanonicalIntegrationConformanceService` 现在记录 `unexecutedAuthorizedActionIds`；授权没有对应终态 execution receipt 时 Integration 为 `incomplete`，完成 `blocked`。
- 本地 repair 删除重复 shell grep；模型 inspection proposal 正确映射 code review；external effect 标志、进程 close 和 runtime mutation action 均恢复 typed 一致性。

### 差异化仿真与门禁

- Extension 193/193 suites、Phase10、Architecture Drift、T6 lifecycle 全通过；架构报告 0 violation，并继续如实列出 6 个既有大文件债务。
- 调度器测试证明 4 个独立观察最大并发 4、结果按调用顺序合并；5 个观察分两波；mixed/effectful 和 canonical metadata veto 保持串行。
- exact-VSIX `agent-fit-review-only` 通过且 changedPaths 为空；`agent-fit-multifile-with-test` 通过两文件写入、Node 聚焦测试、自动语法检查和 readback。
- 详细对标见 `T7-T8-TYPE-ARCHITECTURE-EFFICIENCY-CODEX-ALIGNMENT-20260818.md`；可复现矩阵见 `code/devseek-tests/t7-t8-type-efficiency/EVALUATION.md`。
- 本轮没有重跑 122 秒的 live DeepSeek Web 中型任务，因此不宣称真实 Provider 墙钟时间已经下降；下一次效率专题应先加入分阶段计时和 prompt 字节预算。

## 2026-08-18 2.0.29 T6 生命周期与中型真实编程仿真（前一专题）

### Codex 源码基线与责任映射

- 主基线仍为本地归档 `code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`。
- `codex-rs/core/src/unified_exec/process_manager.rs:524-540` 在 initial yield 前持久化仍存活进程，避免 turn 中断释放最后引用并误杀后台任务；`:1024-1033`、`:1518-1533` 分别处理容量淘汰和全局关闭。
- `codex-rs/core/src/unified_exec/process.rs:207-239,635-638` 将底层进程终止、输出取消和析构回收归到同一 owner。DevSeek 按责任而非 Rust 结构照搬：Bridge 生命周期、浏览器 agent、扩展拥有的 Bridge 进程、捕获命令进程树分别有单一 owner 和幂等 shutdown。
- turn loop、tool router、apply_patch 与完成证据仍沿用 `session/turn.rs:349-405`、`tools/router.rs:154-205`、`tools/handlers/apply_patch.rs:380-446` 的责任边界：模型理解原始自然语言并提出动作，本地逐动作仲裁，工具结果回注，真实证据结算。
- 以上是本地归档源码确认事实。Claude Code 核心循环没有公开可审计完整源码，只能作为官方文档和可观察行为补充。

### T6 架构与实现

- Bridge 新增 `BridgeRuntimeLifecycle`，把请求队列停止、在途请求拒绝、DeepSeek 浏览器关闭、HTTP server close 和 signal/parent-death 收束到一个幂等路径；`DeepSeekAgentRuntime` 串行化初始化、恢复和关闭竞态。
- Extension 新增 `BridgeProcessOwner`、`CapturedProcessRegistry`、`OwnedProcessTreeController`。Unix 使用独立 process group 并递归清理后代，Windows 使用 `taskkill /T`；正常退出、取消、超时和 extension deactivate 都经过同一所有权边界。
- foreground agent 与 background memory 标记明确 workload role，终态和日志只关联相应 run，避免后台任务占用前台完成状态；真实 harness 使用唯一临时根并在每轮检查、温和终止、强制终止、再次确认残留。

### 中型真实用户仿真与根因闭环

- 可复现用例在 `code/devseek-tests/t6-medium-dual/`：676 行 C++17 种子工程、三轮自然语言约束、公开测试、独立隐藏测试、Codex 风格参考解和真实 DevSeek runner。运行工作区与约 265MB 原始证据保留本机但由目录 `.gitignore` 排除。
- 参考解是本次 Codex coding agent 的工程参考过程，不是 Codex 产品 API 跑分；公开/隐藏测试通过，最终 797 行。DevSeek 从同一任务连续执行三轮，最终运行器口径 977 行。
- 真实失败迭代依次暴露：允许修改范围被当成每轮必改目标、旧失败/脏基线污染当前完成、bare JSON 和 ReAct 相邻动作吞入源码、网页空白变化导致 replacement 失配、独立复核把异常隔离误判成无效输入拒绝，以及产品已通过但 harness 对目标文件合同判断错误。
- 根修分别落在 `CodingChangePlan.requiredTargets/authorizedTargets`、source validation ledger、bounded tool protocol adapter、统一 text replacement、requirement review contract 和 benchmark contract；均补同类边界测试，没有为隐藏断言硬编码。

### 最终证据与能力边界

- 最终 Stage 3：`2026-08-18T08:16:51.958Z` 开始，122448 ms 完成；真实 Provider 14 次请求均有终态，10 次工具调用，1 个授权文件提交，Canonical Completion=`completed`。
- `./test.sh` 退出码 0；隐藏验收输出 `T6_MEDIUM_HIDDEN_PASSED`；临时 VS Code、Bridge、Provider 进程 `remaining=[]`。本地摘要为 `code/devseek-tests/t6-medium-dual/reports/devseek-stage3.summary.json`，精炼评估见同目录 `EVALUATION.md`。
- DevSeek 相比参考过程仍更慢、更冗长，DeepSeek 工具方言稳定性也更弱。本轮证明 T1-T6 所覆盖的本地真实流程已闭环，不证明开放世界意图、所有 Provider 或总体能力已经达到 Codex/Claude Code 同级。

### 最终回归与架构收敛

- 聚焦意图/协议/记忆/生命周期/证据矩阵 `363/363` PASS；Shared 全量 `354/354` PASS；Bridge `36/36` PASS；Extension 全量 `192/192` suites PASS；T6 HTTP graceful shutdown 与 extension-host parent exit 两个真实进程场景 PASS。
- 全量回归继续击穿并根修了四类非用例特判问题：XML 方言抽取遗漏公共解析依赖、未决定的验证要求被误标为本地权威、合成 restart context 虚增 session 消息计数、Webview 适配器放在 app 域造成反向依赖。对应补充了解析行为、依赖变更验证、重启持久化和工作状态纯函数执行测试。
- 大文件职责按语义 owner 抽取：tool result contract、XML/JSON protocol dialect、context convergence、final settlement、session persistence、runtime shutdown、task-history Webview adapter 和 Bridge inline-file context 各自独立；`verify:architecture-drift` 保持原预算不放宽并通过。
- `extension:package`、packaged Bridge health check 和本机 VSIX 强制安装均通过。最终安装包在唯一提交形成后重新打包安装；包身份、SHA256、本地安装版本和远端分支由“当前停止点”列出的命令核对。

## 2026-08-18 2.0.28 C++ 真实任务执行与待确认编辑闭环（历史）

### 真实运行证据与根因

- 主任务日志：`.devseek/runs/20260818-014011004-51b2de5edcce4381.log`；“全部保留”侧运行：`20260818-014215768-7a804a3ca0a82d1e.log`、`20260818-014216076-9f13a077ecf2b65e.log`。
- Provider 把嵌套 `<read_file><path>...</path></read_file>` 放进 Markdown 代码块。旧投影策略只检查“没有文件修改工具”，即使本轮已有读取工具也会把 Markdown 当作文件产物；旧协议污染守卫又没有识别嵌套 paired XML，最终把工具文本写进 `code/main.cpp`。
- 模型随后建议删除错误文件。旧 `WriteAuthority` 把模型的 destructive fail-closed 语义提议当成用户任务合同修订，导致 `code/main.cpp`、`code/movie_countdown.cpp` 一并进入禁止范围，后续正常修复被拒绝。
- Provider 用 `g++ -DGOLD='"\033[33m"' ... 2>&1 | head -30` 临时补充未定义的 `GOLD`，所以已有二进制可以手工运行；但 `movie_countdown.cpp` 本身仍不能独立编译。旧非 `pipefail` shell 还可能采用 `head` 的零退出码，把上游编译失败显示为成功。
- 文件写入已经提交，但 `onAppliedChange` 没有携带 `WorkspaceTextFileCommitToken`。点击“全部保留”确实到达后端，却在 `Pending edit keep receipt requires a source commit token` 处失败，Webview 又没有 started/failed 状态，因此用户只看到按钮无反应。
- 最终 Canonical Kernel 能判失败，但自动验证红卡出现后，Todo 要等循环完全退出才结算，形成截图中的短期 `2/2` 全绿不一致。

### Codex 源码对标

- 基线固定为 `code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`。
- `codex-rs/core/src/session/turn.rs:349-405`：模型输出、工具调用和工具结果在同一 turn loop 中推进；DevSeek 保留原始响应，只把规范化工具送入执行边界。
- `codex-rs/core/src/stream_events_utils.rs:296-382`、`tools/router.rs:154-205`：结构化工具请求触发工具工作，普通 assistant 文本不自动成为工具效果；DevSeek 仅在本轮完全没有规范化工具时启用 legacy Markdown 文件投影。
- `codex-rs/core/src/tools/handlers/apply_patch.rs:380-446`：具体动作先解析/验证，再产生可审计 effect；DevSeek 不再允许模型 destructive 提议改写用户授权范围，具体 delete 仍逐动作进入本地审批和 sandbox 仲裁。
- 以上是已由本地归档源码确认的事实。Claude Code 核心循环不是公开可审计源码，只保留为官方文档和可观察行为补充，不能声称内部实现一致。

### 实现边界

- 所有 create/replace/delete/Markdown/simple-file 写入生产者统一向 `AppliedChangeRecord` 提供必需 `commitToken`；缺失凭据在生产边界失败，不再把无来源记录交给待确认队列。
- `PendingEditCoordinator` 统一封装 hunk/file/all 的 keep/undo 操作反馈；Webview 显示“保留中/撤销中”、禁用重复点击，并在失败时保留队列、显示原因和 VS Code 错误通知。
- `generated-file-parser` 拒绝注册工具的嵌套 paired XML；`markdown-artifact-tool-projector` 只在工具数组为空时兼容投影。
- `WriteAuthority` 保持用户合同为授权源；模型 destructive fail-closed 提议只是动作证据，不能污染后续合法修复范围。
- validation execution profile 在 Unix Bash 上启用 `pipefail`，上游 compiler/test 非零退出码不会被 `head`、`tee` 等末端命令掩盖。
- `AgentTerminalPresentationBuffer` 在收到 `validate:failed` 时立即发布带 `__agentState` 的权威失败 Todo；后续真实修复并通过验证后，最终完成证据可覆盖它。

### 仿真与验证

- 聚焦跨层矩阵 `67/67` PASS：嵌套读取协议不落盘、无工具 legacy 产物仍投影、模型删除提议不撤销用户修复范围、validation pipeline 上游失败保持非零、Keep All 成功清队列、凭据异常可见且保留队列、验证失败立即取消全绿 Todo。
- 删除路径原严格断言已升级为提交凭据契约，并单独 `39/39` PASS。
- Extension 全量 `189/189` suites PASS；包含 45 类意图行为、52 类路由合同、多语言/错字输入、连续 steering、Provider 协议偏离、T1-T5 跨 session/进程重启、中大型任务和运行证据回放。
- `npm run compile --workspace=packages/vscode-extension` PASS；`npm run extension:package:release` PASS，生成 `devseek-netai-2.0.28.vsix` 与 `devseek-netai-latest.vsix`。
- 独立 `npx tsc --noEmit -p packages/vscode-extension/tsconfig.json` 仍有 2.0.27 之前已存在的 agentic-loop/provider/task-contract/lexicon 类型债务；esbuild 正式编译和本次改动文件无新增类型错误。后续应建立独立类型债务专题，不能误写成 2.0.28 全量 TypeScript 类型检查通过。

## 2026-08-17 2.0.27 T1/T2 记忆隔离与权威终态（历史）

### 截图根因

- 等待 Provider 首个 delta 前没有即时反馈，用户只能看到等待动画。
- 模型循环在 Canonical Kernel 结算前发布 done、最终回答和全完成 Todo；后续失败会造成 Todo 全绿但底部报红。
- Completion 已能识别恢复写入，C8 code-change 却仍把任何历史 failed mutation 永久判失败，造成投影错误。
- strict memory 无匹配时回退 global summary，导致新 session 普通知识问答携带 UAV/MAVLink 上下文。

### Codex 对标与实现

- 基线固定为 `code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`。
- 对标 `codex-rs/core/src/tasks/mod.rs:772-807`：terminal error 与 task body 先结算，再发 `TurnComplete`；DevSeek 由 `AgentTerminalPresentationBuffer` 缓冲所有终态声明。
- 对标 `codex-rs/core/src/tools/handlers/plan.rs:84-95`：plan/Todo 是过程事件，不授权任务成功。
- 对标 `codex-rs/tui/src/app/thread_events.rs:124-133`：UI active turn 只随权威 completed 事件清除。
- 对标 `codex-rs/core/src/thread_manager.rs:331-333,859-966`：新 thread 与 resume/fork 分离；DevSeek strict context 无匹配不再注入 global summary。
- `coding-code-change.ts`、完成裁决和投影复用恢复证据规则：失败写入必须被后续提交、读回和通过验证覆盖；否则仍 fail-closed。
- Provider 请求立即显示等待状态，每 5 秒更新等待时长，首个 delta 后切换为解析状态；内部错误转换为用户安全说明。

### 仿真与产物

- exact-VSIX `t1-new-session-memory-isolation-product`：UAV 记忆、进程重启、新 session GPU/CPU 问答，Provider prompt 与用户可见日志均无污染，PASS。
- exact-VSIX `t2-terminal-settlement-recovery-product`：真实 failed mutation、恢复提交、`g++` 编译运行、readback、自动语法验证、Kernel/Todo 一致 completed，PASS。
- controlled VSIX prompt contract 104/104 PASS；Shared 专项 48/48、全量 346/346 PASS；VS Code Extension 全量 188/188 suites PASS。
- 当前包：`devseek-netai-2.0.27-debug.20260817.t192306.gf2f84d4.vsix`，SHA256 `3873ae540ab76ea31fe98f552f1c062a9df6b1927d0a1280c1b33c619299bd03`。
- 专题设计：`DEVSEEK-2.0.27-CODEX-TERMINAL-AUTHORITY-AND-MEMORY-ISOLATION-20260817.md`。

## 2026-08-17 2.0.26 T1 可见交付与仿真检错能力

### 截图根因

- `说明gpu cpu` 的 assistant answer 已由 Provider 生成，但 model-led 不发布本地 plan 事件，Webview 却仍等待 `plan:completed` 才显示 delta，因此用户看不到回答。
- 旧仿真仅验证运行日志中存在 Provider answer，没有验证 Webview DOM，所以错误通过。

### 实现与验收

- `AgentTurnPresenter` 区分 `progress` / `direct-response` / `model-led`，前两类非进度回合在真实工具前抑制本地伪状态，但内部 run evidence 仍完整记录。
- Webview 直接渲染 model-led/direct assistant delta；真实 read/write/terminal activity 会动态升级进度 UI。
- DOM 矩阵 13 类输入全部精确可见，无伪调查卡；工具升级 case 通过。
- 同 session 追问 `说明gpu cpu` -> `再详细说明他们的差异` 已加入模型上下文、DOM 和 exact-VSIX 三层仿真；exact-VSIX 2/2 PASS，第二轮确认携带首轮问答，零工具、零写入。
- mutation sensitivity：临时恢复 `agentPlanDone=false` 旧缺陷后，可见交付 case 按预期失败，证明 oracle 真能发现该缺陷。
- Extension 全量 184/184 suites PASS；exact-VSIX DOM PASS；`t1-direct-answer-product` 5/5 PASS。
- 设计：`DEVSEEK-2.0.26-CODEX-VISIBLE-DELIVERY-AND-SIMULATION-QUALITY-20260817.md`。
- 报告：`docs/testing/devseek-20260817-t1-visible-delivery-2.0.26.md`。

## 2026-08-17 2.0.25 T1 助手消息与意图结算

### 截图结论

- `解释gpu cpu` 的持久化语义是 `taskKind=explain`，所以不是 T1 初始分类失败。
- `workflowMode=model-led` 被错误换算成 `edit`，导致普通问答进入工程执行结算。
- Provider 短回答又被本地长度/结论关键词启发式分类为 `short_intent`，最终显示执行失败。
- `agent-run-display.ts` 还在模型动作出现前发布“已确定软件工程执行路线”，造成可观察行为与真实意图不一致。

### Codex 源码基线

固定快照仍为 `code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`。

- `codex-rs/core/src/session/turn.rs:140-151` 明确区分 function calls 和 assistant message：工具调用执行并回灌；只有助手消息时记录并完成 turn。
- `codex-rs/core/src/stream_events_utils.rs:296-360` 对工具调用设置 follow-up，对普通非工具消息保存 `last_agent_message`，没有语言、字数或结论关键词门禁。
- `codex-rs/core/src/session/turn.rs:482-488` 在没有 follow-up 时以最后助手消息结束 turn。
- Claude Code 核心 loop 不公开；官方 CLI 文档中的 explain/query 行为只作为公开补充，不写成源码事实。

### 2.0.25 实现

- `provider-output-integrity.ts` 只检查空响应、截断、登录/错误页、工具协议和 transcript 污染；普通结构有效助手消息为 `complete_answer`。
- `completion-evidence.ts` 的 read-only delivery 不再依赖字数、语言、标题或结论词。
- `agent-runtime-state-machine.ts` 和 `agentic-loop.ts` 从真实 workspace/external effect receipt 与 task completion 观察推导 respond/edit，路由预测不再是 authority。
- `agentic-system-prompt.ts` 与 `engineering-guidelines.ts` 保留 model-led 下的 chat 合同，普通问答允许直接回答并跟随用户语言。
- `agent-run-display.ts` 复用当前 semantic contract；普通 QA 不发布虚假工程计划，其他未知任务使用中性 model-led 状态。
- `t1-direct-answer-product` 已加入默认、intent 和 acceptance controlled suite。

### 验收

- 专项核心：310/310 PASS。
- 显示与架构专项：288/288 PASS。
- 全量：182/182 suites PASS。
- exact-VSIX `t1-direct-answer-product`：4/4 PASS，覆盖中文短句、中文错别字口语、英文和日文；全部零工具、零写入，且旧工程路线文案不存在。
- exact-VSIX `independent-user-diversity-product`：10/10 PASS。
- 详细设计：`DEVSEEK-2.0.25-CODEX-ASSISTANT-MESSAGE-INTENT-SETTLEMENT-20260817.md`。
- 仿真报告：`docs/testing/devseek-20260817-t1-intent-direct-answer-2.0.25.md`。

## 2026-08-17 2.0.24 T1-T5 纵向收敛（前序基线）

### 本轮新增用户旅程

`t5-medium-program-session-restart` 使用真实安装 VSIX 和四轮用户输入完成一个保留在证据目录中的 Node.js 中型任务板项目：

1. 新建主 session，创建 store/service/test 并执行真实 `npm test`。
2. 新建无关 session，只读检查独立文档；不得继承主任务标记、文件或写权限。
3. 返回原主 session，以省略式中文追加 CLI、报告和优先级筛选；必须保留旧 API 和既有成果。
4. 重启 VS Code 扩展进程，再返回同一 session，增加原子 JSON 持久化并用第二个 service 实例验证重启恢复。

最终 retained workspace：`code/devseek-tests/top-agent-convergence/runs/20260817-t1-t5-memory-session-complete-2.0.24-final/t5-medium-program-session-restart.workspace/`。它包含 `package.json`、README、5 个 `src/*.js` 文件和真实测试文件，不是只检查模型回复文本。

### 仿真发现的根因与类别修正

| 失败探针 | 根因 | 系统性修正 |
|---|---|---|
| 返回原 session 时模型看不到第一阶段标记 | session relevance 只按当前轮词汇过滤；省略式追加要求与旧任务词汇不同 | `agent-session-context.ts` 建立统一投影策略：无显式新文件作用域的明确 continuation 保留有界历史和 working set；显式文件仍收窄，新 session 仍绝对隔离 |
| 后台 memory 完成事件被当成前台用户任务终态 | 测试和 UI 观察只按“最新 terminal”选取，没有使用 run source 身份 | memory Provider 使用独立 `DevSeekRunContext`、run/operation id 和 settlement；所有产品终态选择排除 `vscode-extension.memory-pipeline`，前后台回调不复用 |
| 独立 review 错把 `package.json` 当作源码，后续又缺少优先级/原子写入的事实 | review fixture 从 primary artifact 猜源码范围，通用桥还了解具体场景 plan | 场景从最终 source snapshot 声明 `independentReviewPaths` 和冻结的 `independentReviewEvidenceFacts`；通用桥只校验/消费契约，不含中型任务 case 分支 |
| 父仓库同名脏文件干扰保留工作区中的独立项目 | Git dirty observer 在父仓库范围取状态，未投影到嵌套 workspace | 状态命令以 workspace 为 cwd、用 `-- .` 限定 pathspec、关闭 rename 合并并把 repo path 投影回 workspace；同名父级文件回归测试锁定该类别 |
| 模型可引用字符串形式的 evidence ref 提升为 `tool-verified` | ref 存在性不能证明来源 authority、epistemic status 和 outcome | pipeline v2 增加不可变 evidence catalog；Phase 1/2 候选必须与真实 tool/verification descriptor 匹配，网络/assistant 证据不能升级为本地成功；有限重试终态为 `exhausted`，v1 状态显式迁移 |
| 多个 rollout 的内建 evidence ref 发生碰撞 | `user:turn:N` 和 `assistant:summary` 只在单个 rollout 内唯一，Phase 2 合并多个 catalog 时可能串用来源 | `memory-evidence.ts` 成为 evidence identity 的唯一 owner，生成 `rollout:<id>:...` 引用；加载 v1 时同时规范化 pending catalog 和已成功 Stage1 output 的 candidate refs，跨 rollout 唯一性与升级路径都有回归测试 |

这些修正都落在 session projection、run identity、source inventory、worktree observer 和 memory evidence authority 的唯一 owner 中。仿真 case 只是发现问题的探针，没有把 prompt 文本或 case id 写入产品判断。

### 最终验收

- shared 全量：`341/341 PASS`；extension 全量：`182/182 suites PASS`。
- 中型项目聚焦 r5：`20260817-t1-t5-medium-program-2.0.24-focused-r5` PASS；4 个场景、一次真实进程重启、同一主 session 身份恢复，prompt contract 自检全部通过。
- 全面 run：`20260817-t1-t5-memory-session-complete-2.0.24-final` PASS；15/15 steps、63 targeted、56 controlled executions（55 unique controlled case ids）、118 selected、94 required、43/43 dimensions，missing execution evidence 为 0。
- 最终报告：`docs/testing/devseek-20260817-t1-t5-memory-session-complete-2.0.24-final.md`。聚焦报告：`docs/testing/devseek-20260817-t1-t5-medium-program-2.0.24-focused-r5.md`。
- 最终本地包以仓库根目录 `devseek-netai-latest.vsix` 为准。收尾必须在最终提交身份确定后重新 package/install，并用 `git log -1`、`code --list-extensions --show-versions` 和 `sha256sum` 现场核对；文档不硬编码会随 amend 改变的递归提交身份和包哈希。

## 2026-08-17 T5 记忆与重启迭代（架构基线）

### 上游对标结论

- Codex 固定基线：`code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`，对标 `codex-rs/memories` 的两阶段写入、租约/重试/no-output、串行 consolidation、小摘要+索引+明细按需读取、引用/使用回执以及 session resume 分离。
- Claude Code 公开仓库 @ `be90077c6a353f292fa612d97173865a9ab21b83` 不含可审计的核心 auto-memory 实现。只使用官方 memory 文档确认机器本地存储、`MEMORY.md` 索引、topic file 按需读取等公开行为，不声称读到闭源内核。
- 详细源码映射：`UPSTREAM-AGENT-SOURCE-AUDIT-20260813.md` 的“2026-08-17 T5”章节。

### 最终架构

1. 主模型继续理解原始用户输入、错字/同音字/口语和 steering；词表不拥有写入或执行权。
2. 任务终态把原始 turn、steering、工具和验证 receipt 写入幂等 pipeline job。Phase 1 模型提取允许 no-output，Phase 2 串行整合，模型输出只是建议。
3. `MemoryService` 和 shared `CodingMemoryPolicy` 作为本地唯一仲裁者，管理 secret、来源、外部提权、作用域、时间、冲突、TTL、撤销和使用回执。
4. auto-memory 位于 VS Code 机器本地 global storage，用 Git common dir 识别仓库；worktree 共享、不同仓库隔离，不污染 workspace。
5. 读取路径是小摘要常驻、索引搜索、少量 rollout 明细按需读取。当前用户/项目规则/工具证据永远高于历史记忆。
6. 显式 `memory_write` 保留为 Codex 同类 ad-hoc 通路，但不对自治模型广告。它被建模为 `local-state` external effect，必须经过用户确认、prepared authority、external-effect settlement、ProductMutation evidence 和 active readback。
7. session state 继续由 `SessionService` write-through/flush 精确恢复，长期记忆不得代替原会话历史。

### 实现与验收

- 主要新边界：`repository-memory-location.ts`、`memory-pipeline-store.ts`、`memory-semantic-model.ts`、`memory-projection.ts`、`memory-pipeline-service.ts`、`evidence-aware-memory-write.ts`。
- shared 全量 `340/340 PASS`；extension 全量 `181/181 suites PASS`。
- T5 focused exact-VSIX run `20260817-t5-memory-focused-r3` PASS，证明显式记忆写入、扩展宿主退出、新进程恢复和当前只读约束优先级。
- 默认全面 run `20260817-t5-memory-acceptance` PASS：14 个步骤、13 个 controlled suites、107 个 selected case，required 83，missing suites/dimensions/execution evidence 全部为 0。
- 最终报告：`docs/testing/devseek-20260817-t5-memory-acceptance.md`。专题设计：`T5-MEMORY-CODEX-ALIGNMENT-ARCHITECTURE-IMPLEMENTATION-20260817.md`。可复用工程决策方法：`MEMORY-ENGINEERING-DECISION-RATIONALE-20260817.md`。

### T5 边界与下一步

- 本轮是本地 T3 可观察行为验收，不是 C14 release qualification。
- T5 已完成，当前按用户要求暂停。不要无原因重跑 107 case，也不要自动进入 T6。
- 未来用户明确继续时，下一专题为 T6“卡顿与资源回收”，重点检查 VS Code 窗口、浏览器、Bridge、后台 memory Provider、日志关联和进程回收。

## 2026-08-14 T4 权限与写入边界迭代（前一专题）

### Codex 源码对标结论

- 固定源码：`code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`。
- `codex-rs/core/src/tools/handlers/apply_patch.rs:235-322`：依据 patch 的实际路径、cwd、session/turn grant 与 filesystem sandbox policy 计算有效写权限。
- `codex-rs/core/src/exec_policy.rs:726-810`：命令 effect 结合危险命令、approval policy 与 sandbox 决策，本地执行合同不能被模型自述替代。
- DevSeek T4 采用对应分层：模型提出具体动作，本地语义合同仲裁 target，shared canonical path boundary 裁决 lexical/realpath，写入提交前重验 route identity，真实工具与磁盘结果提供完成证据。
- Claude Code 本轮只采用公开可观察行为作为补充基线，不宣称逐行对标不可核验的核心源码。

### 本轮实现

- 将 extension 内重复的 path containment 下沉到 `packages/shared/src/coding-workspace-path-containment.ts`，删除旧 owner；artifact、todo ledger、Markdown deliverable、write policy 与 workspace edit 统一依赖 shared contract。
- 新增 `coding-workspace-path-boundary.ts`：同时验证 lexical path 与最近已存在祖先的 realpath，拒绝工作区软链接逃逸，并保留提交前路径换路重验。
- terminal command policy 使用同一 canonical boundary 检查 workdir、绝对路径和既存相对路径；process substitution 与动态目标展开（包括变量本身是完整路径）fail closed，纯环境变量显示仍可低摩擦执行。
- 首次真实仿真发现“basename prompt target + 完整 model tool path”被重复计算为两个交付物。语义合同服务现由具体工具路径精化模糊 basename，同时保留不同目录同名目标的独立性。
- Controlled VSIX harness 新增 `t4-permission-write-boundary`：覆盖无害子目录创建、源码只读但允许报告 artifact、工作区外写入拒绝、危险 shell 拒绝。

### 验收证据

- 首次 focused exact-VSIX 按真实断言失败并暴露上述语义缺陷：`docs/testing/devseek-20260814-t4-permission-write-boundary.md`。
- 最终包 focused exact-VSIX：`4/4 PASS`，报告 `docs/testing/devseek-20260814-t4-permission-write-boundary-focused-rerun.md`。
- 一次完整 final run 出现跨 case terminal/run-log 关联抖动；失败套件 `coding-conformance-product` 单独 fixpoint 重放后 `10/10 PASS`，报告 `docs/testing/devseek-20260814-t4-permission-write-boundary-acceptance-final-focused-rerun.md`。该现象已登记为 T6 输入。
- 最终默认 acceptance exact-VSIX recheck：`95` 个 selected case、`71` 个 required acceptance case、`12` 个 controlled product suites 全通过；`36/36` 维度覆盖，missing dimensions `0`，execution evidence missing `0`。报告 `docs/testing/devseek-20260814-t4-permission-write-boundary-acceptance.md`。
- 路径/终端/controlled 首轮合同 `100/100 PASS`；写权限及 sibling 回归 `325/325 PASS`；runner/controlled contract `91/91 PASS`；语义目标精化及相关合同 `168/168 PASS`。
- extension compile/package、latest VSIX 安装和 packaged bridge verification 均通过。
- 详细专题说明：`T4-PERMISSION-WRITE-BOUNDARY-CODEX-ALIGNMENT-20260814.md`。

### T4 边界

- DevSeek 当前没有 Codex OS-level sandbox 的等价实现；现有保证来自 canonical path、tool authority、terminal policy 和提交前重验，必须如实描述为本地 fail-closed 合同。
- T6 已知输入：本轮完整验收观察到一次文件结果正确但 terminal 误关联到后续 case 的 run-log 抖动；focused 与完整 recheck 均通过。T6 应核查窗口切换、run correlation 和日志选择。
- T4 已完成。除非后续改动触及 shared path boundary、write authority 或 terminal policy，不要无原因重跑本轮 95 case。
- 当前暂停；用户明确继续后，下一个专题是 T5“记忆与重启”，不得自动开始。

## 2026-08-14 T3 DeepSeek Web 兼容迭代（前一专题）

### Codex 源码对标结论

- 固定源码：`code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`。
- `codex-rs/core/src/tools/router.rs:153-205`：模型 `FunctionCall`、`ToolSearchCall`、`CustomToolCall` 归一化为统一 `ToolCall`。
- `codex-rs/core/src/tools/registry.rs:520-590`：未知工具、payload 不兼容、pre-tool hook 阻断都有显式错误；可恢复错误返回模型，不沉默完成。
- `codex-rs/core/src/tools/parallel.rs:72-85`、`:230-245`：非致命工具失败转换为模型可见 failure response。
- DevSeek T3 因此采用同类契约：DeepSeek Web 的 Markdown、坏 JSON、OpenAI wrapper 变体和 provider footer 先进入本地工具协议归一化；可恢复工具请求继续执行并产生 evidence，无法恢复/截断则 fail closed。
- Claude Code 核心源码不作为本地硬证据；后续只可把公开可观察 coding-agent 行为当补充基线，不能写成已逐行对标源码。

### 本轮实现

- `fake-tool-parser.ts` 支持恢复 quote-damaged OpenAI `tool_calls` wrapper 中的单个或多个工具调用。
- `parseJsonObjectToolCalls` 支持扫描 Markdown 编号列表中的多个 standalone JSON 工具对象。
- `stripToolCallBlocks()` 会剥离已恢复的工具 payload，保留自然语言，不让协议 JSON 泄漏到最终回答。
- 保留普通 artifact JSON array 防误判保护，避免把报告内容误当工具执行。
- Controlled VSIX harness 新增 `t3-deepseek-web-compat` 套件：
  - `t3-deepseek-malformed-openai-tool-calls`
  - `t3-deepseek-markdown-json-tool-list`
- Top-agent runner 将 T3 套件加入默认 controlled suites 和 acceptance suites，新增维度 `deepseek_web_tool_json_compatibility`，强制验收 case 由 65 增至 67。

### 验收证据

- 精确候选包：`devseek-netai-2.0.23-debug.20260814.t170131.g19cee47.vsix`；dirty runtime fingerprint 覆盖 `packages/vscode-extension/src/agent/fake-tool-parser.ts`。
- Parser + provider integrity：`138/138 PASS`。
- Runner contract：`12/12 PASS`。
- Controlled VSIX prompt contract：`74/74 PASS`。
- `npm run extension:package:debug` PASS；`code --install-extension devseek-netai-latest.vsix --force` PASS；`npm run verify:packaged-bridge` PASS。
- T3 focused exact-VSIX run：`20260814-t3-deepseek-web-compat` PASS，报告 `docs/testing/devseek-20260814-t3-deepseek-web-compat.md`。
- 默认 acceptance exact-VSIX run：`20260814-t3-deepseek-web-compat-acceptance` PASS，报告 `docs/testing/devseek-20260814-t3-deepseek-web-compat-acceptance.md`。
- 最新 acceptance：selected case `85`，required acceptance case `67`，covered dimensions `34/34`，missing dimensions `0`，execution evidence missing `0`。
- 详细专题说明：`T3-DEEPSEEK-WEB-COMPAT-CODEX-ALIGNMENT-20260814.md`。

### 后续 T3 缺口

- 半截工具调用：继续保持 fail closed、零写入、有界重试。
- 重复命令：确认错误回灌后不会无限重复执行同一失败动作。
- 错误后恢复：第一次 malformed 工具失败后，下一轮有效工具请求仍可完成。
- 真实 DeepSeek Web live sample：只有用户授权保留窗口和页面时执行，并单独标注为 live Provider evidence。

## 2026-08-14 2.0.23 独立用户仿真收敛（T1/T2 基线）

### 最终过程架构

- 原始用户输入直接进入主模型；错别字、同音字、口语、省略和中英混输由模型恢复，不用关键词先决定任务。
- `model-tool-semantic-proposal.ts` 从模型实际工具名和参数形成 mutation/run/read/external/destructive 提案。
- `write-authority.ts`、`task-semantic-contract-service.ts` 和共享 Kernel 只按实际 action/target 做 workspace、scope、risk、approval、sandbox 与 safety 仲裁。
- `intent-revision-lineage.ts` 把模型 proposal 绑定到当前用户 revision；新输入替换冲突的旧 pending work，已提交效果保留。
- completion adapter 把普通回复/历史证据与 verifier evidence 分离，只有 TaskContract 要求验证时才强制验证 receipt。
- callback 复制保留动态 getter，避免 Kernel 看到过时的 TaskContract、requirements 或 authority。
- 多语言 lexicon 可动态追加高置信边界表达，但只提供本地约束证据；“不要引入依赖”不会再被反向解释为外部 effect 请求。

### Codex 源码过程基线

- 固定源码：`code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`。
- `session/turn.rs:273-336`：pending input 在同一采样循环进入下一 step，context、advertised tools 和 tool calls 共享一次 step view。
- `tools/router.rs:153-205`：模型 function/custom call 规范化成统一 `ToolCall`。
- `tools/registry.rs:474-658`：实际 payload 经 pre-tool hook、runtime dispatch，阻断或失败返回模型。
- `tools/handlers/apply_patch.rs:275-322`：从实际 patch 路径计算权限；`exec_policy.rs:726-799` 从实际命令、approval 和 sandbox 推导决策。
- DevSeek 因此不按整段 prompt 预冻结全部文件工作；本地层约束模型真正提出的动作和目标。Claude Code 核心未公开，继续只作公开行为补充基线。

### 最终验收

- 精确候选包：`devseek-netai-2.0.23-debug.20260814.t161656.gce592f4.vsix`，compile/package/install PASS。
- 最终 run：`20260814-independent-user-comprehensive-2.0.23-final-acceptance-recheck`，11/11 step PASS。
- 19 个 targeted test file PASS；10 个产品套件、44 个真实流程 case 全部执行 PASS。
- runner 选择 83 个 case，强制验收 65 个；覆盖维度 33/33，缺失维度 0，执行证据缺失 0。
- Shared 全量：336/336 PASS；范围替换专项 2/2；编码一致性专项 10/10。
- 详细结论：`INDEPENDENT-USER-SIMULATION-ACCEPTANCE-20260814.md`。
- 本轮按用户要求完成后暂停；一个提交、一次 push、不打 tag。

### 资格边界

本轮可声明：在用户认可的本地 independent exact-VSIX 仿真矩阵内，意图识别、过程处理和任务完成达到 Codex/Claude Code 对标的可观察行为验收线。不能声明：内部实现相同、真实 Provider 已认证、C14 或正式 release qualification 已通过。

## 2026-08-14 2.0.22 动态 TaskContract 修订迭代（历史）

### Codex 源码对标结论

- `code/upstream-agent-sources/openai-codex/codex-rs/core/src/session/input_queue.rs:19-80`、`:268-367` 使用 turn/session scoped queue 按顺序保存和 drain pending user input，不把后续输入压成一次静态意图分类。
- `code/upstream-agent-sources/openai-codex/codex-rs/core/src/session/turn.rs:245-405` 在同一主模型循环中消费 pending input，并在采样/工具结束后根据 pending input 继续 follow-up。
- `code/upstream-agent-sources/openai-codex/codex-rs/core/src/tasks/regular.rs:74-90` 表明常规任务不会因一次 agent message 就忽略已到达的新用户输入。
- 直接对标后发现 DevSeek 2.0.21 的缺口：WriteAuthority 已能更新模型语义修订，但 Canonical Coding Kernel 的 TaskContract、ContextGraph、Requirements、Design、ChangePlan、权限 receipt、验收与 completion 仍可能停留在初始版本。这会导致模型听懂了“不做 alpha，改做 beta”，但本地工具或结算仍使用 alpha。

### 2.0.22 已实施

- 新增 `packages/shared/src/coding-task-contract-revision.ts`，由 `CanonicalTaskContractRevisionService` 持有当前不可变 TaskContract 快照、有序修订 receipt 和冲突身份检查；同 ID/同内容幂等，同 ID/不同内容 fail closed。
- `CanonicalCodingKernel` 把该 revision session 作为动态 task source 注入 ChangePlan、ToolAuthority、Verification 和 VerifierSelection；修订后重算 ContextGraph、Requirements、Design 与 ChangePlan，最终只按最新 TaskContract 释放和结算。
- 旧 TaskContract 下尚未执行的 action/authority receipt 会以 `stale-task-contract-action` 或 `stale-task-contract-receipt` 失效；旧验证 receipt 不能满足新契约的 acceptance。
- VS Code adapter 在每次 drain steer 后投影新的结构化目标、禁止项和 strict scope，不再从包含“放弃 alpha”的纠正原文中把 alpha 重新抽成 deliverable。
- `intent-revision-lineage.ts` 保留独立的历史禁止项，只有用户显式重新授权指定目标时才移除；被新要求取代的旧 pending target 转为 prohibited，已提交效果仍作为事实保留。
- correction、scope reduction、steer、negation、replacement 和 reauthorization 的高置信本地语言信号已迁入 `operational-language-lexicon.ts`，可用 JSON 动态追加新语言/表达；`intent-revision-lineage.ts` 不再硬编码这些中英文正则。该配置只提供显式约束证据，不取代主模型的自然语言理解。
- 隐式编程任务的 conformance fixture 显式携带“模型已理解的结构化任务事实”，TaskContract resolver 不为通过测试而新增 `red/green/挂了` 之类关键词。

### 2.0.22 验证证据

- Shared 全量回归：`331/331` PASS。
- 意图专项本地契约：`139/139` PASS；仿真运行器单测：`12/12` PASS。
- 打包内 Bridge 验证：`npm run verify:packaged-bridge` PASS。
- 最终专项 run：`20260814-intent-2.0.22-task-contract-revision-final`，`4/4` step PASS；包含 29 个本地意图 case、3 个受控 VSIX suite 和 6 个同会话用户输入 case。
- 三类产品场景为 prior-task continuation、alpha 到 beta 的 scope replacement、mutation 到 read-only review 的 cancellation replacement；全部 Bridge 请求和最终工作区断言通过，无高资源异常和残留 VSIX 进程。
- 报告：`docs/testing/devseek-20260814-intent-2.0.22-task-contract-revision-final.md`；证据根：`code/devseek-tests/top-agent-convergence/runs/20260814-intent-2.0.22-task-contract-revision-final/`。
- 这是 `intent-recognition-regression` 专项证据，按要求没有重复运行通用 coding journeys，也不产生 C14/release qualification 声明。

## 2026-08-14 2.0.21 Codex 式模型主导意图迭代（历史）

### 必须延续的架构决策

- Codex 本地源码主基线：`code/upstream-agent-sources/openai-codex`，固定提交 `fe614a6304ef804be74a622e482fdd75977abcba`。
- Claude Code 公开补充基线：`code/upstream-agent-sources/anthropic-claude-code`，固定提交 `be90077c6a353f292fa612d97173865a9ab21b83`。核心 agent loop 未公开，不得声称逐行复刻。
- 原始用户输入必须先进入正常主模型 turn。模型结合上下文理解中文错别字、同音字、口语、省略、ASR 风格和中英混输，并决定直接回答、澄清、调查或提出工具动作。
- 本地关键词/多语言配置只用于提取显式禁止项、目标、风险和兼容诊断；不得决定最终意图、阻止主模型运行或直接授予写入/终端/外部 effect 权限。
- 本地层按模型实际提出的具体动作仲裁 workspace containment、显式 include/exclude、protected path、risk、approval、sandbox 和 safety policy。
- 工具成功、失败、拒绝与确认结果回到同一模型循环；changed paths、readback、命令退出码、验证结果和 effect receipt 决定是否完成。
- 活跃任务允许用户多次补充、纠正、换目标、取消或收窄要求。最新 steer 在下一次采样前生效，冲突的旧未执行工具提案作废；已经提交的效果作为事实保留。

### 2.0.21 已实施

- `workflow-service.ts` 将非空 agent turn 统一为 `model-agent / model-led`，不再让关键词路由提前替代主模型。
- 删除执行前语义分类/确认旁路及 `interaction-service.ts`；`semantic-route-service.ts` 只保留模型 turn 的本地路由提示，不再额外调用分类模型。
- `agentic-loop.ts` 在 model-led 模式下取消 Markdown/simple-file 预执行快捷路径；没有工具时可直接回答，实际 work tool 出现后才建立相应证据义务。
- `agentic-system-prompt.ts` 明确要求理解带噪自然语言，不要求用户先纠正字词或按固定格式输入。
- `coding-tool-authority.ts` 增加 model-led 动作级权限策略；模型可提出动作，但显式 scope、排除、风险、确认和安全规则仍由本地裁决。
- `agent-file-write-policy.ts`、`write-authority.ts`、`intent-revision-lineage.ts` 支持模型提案、实时 steer、最新目标替换及已提交效果保留。
- steer 会在 Provider 采样前后 drain；新要求到达后丢弃旧的未执行工具提案并重新采样。
- `SessionService` 成为同一进程内 agent state 的 read-your-writes owner；扩展复用单一服务实例，异步 `workspaceState.update()` 不再使紧接着的 `go ahead` 读到旧契约。
- Coding Kernel 的验收执行模式来自 `semanticContract.intent.mode`，`model-led` 只表示编排权归主模型。计划轮仍只读，批准后的 edit 轮才建立源码修改和验证合同。
- `task-contract.ts` 不再把纠正边界之前的历史目标复活为当前 deliverable；model-led 初始 UI action 固定为无副作用的 `explore`。

### 专项仿真入口与结果

- 后续意图识别迭代使用：

```bash
node scripts/devseek-top-agent-user-simulation-runner.mjs --force --intent-only --run-id <run-id> --markdown docs/testing/devseek-<run-id>.md
```

- `--intent-only` 只执行 6 个意图相关本地测试文件和 3 个真实 VSIX 多轮 suite，不重复 `journey-core`、`realistic-product`、`coding-conformance-product` 等通用编程 case。
- 最终专项 run：`20260814-intent-2.0.21-model-led-intent-only-final`，`4/4` step PASS。
- 本地覆盖 `24` 个意图 case：动态多语言配置、主模型 turn ownership、错别字/同音字、直接问答无工具、真实写入/readback、实时 steer、只读/执行/外部 effect 仲裁。
- 真实 VSIX 覆盖 `6` 个多轮 case：先方案后批准、改口换目标、取消并改为只读；全部 PASS，execution evidence missing 为 `0`。
- 单独失败类重放 `20260814-intent-2.0.21-model-led-local-acceptance-prior-continuation-final` 已 PASS，确认 `go ahead` 同时继承目标、执行权限和验证合同。
- `verify:packaged-bridge` PASS；Extension compile 与 shared build PASS；收尾流程已在最终提交后重新打包并安装 VSIX，使 build id 与提交 SHA 一致。

### 已知验证边界

- 本轮开始时完整 Extension suite 为 `177` suites、`172` PASS、`5` FAIL；相同 5 项在干净基线也失败：`completion-evidence`、`markdown-deliverable-flow`、`markdown-deliverable-task`、`run-context`、`run-log-replay`。它们属于 source-fact/Markdown evidence chain 的既有问题，不是本轮意图回归。
- 用户已要求意图专项不要同时重复通用 case，因此本轮最后验收以 `--intent-only` PASS 为准；不要把它误写成完整发布资格。
- 当前任务完成后暂停。下一轮若继续优化其他处理，仍先查本地 Codex 源码，再按同样的“主模型决策、动作级本地仲裁、证据闭环”边界实施。

## 2026-08-14 2.0.20 历史交接快照

### 已完成并推送

- 最新提交：`338fac0 fix(intent): keep advisory action questions read-only`。
- 远端：`origin/devseek-multi` 已确认指向 `338fac01736cc7cbe99c0bd8f12e6c4fa15efd2b`。
- 版本：`2.0.20`，未打 tag。
- VSIX：`devseek-netai-2.0.20-debug.20260814.t104347.g338fac0.vsix` 已打包并本地安装。
- 2.0.20 行为：咨询式动作问题只读边界已完成。下列表达不再触发源码修改或终端执行：
  - `What changes are needed to fix src/login.ts?`
  - `Can you explain how to implement caching in src/cache.ts?`
  - `Should we refactor src/api.ts or leave it as-is?`
  - `src/login.ts 应该怎么改？`
  - `What command should I run to test this project?`
  - `Should I run npm test before changing anything?`
- 2.0.20 仲裁：即使模型语义候选误判为 `existing-project-edit` 或 `terminal-validation`，本地契约看到 `advisory-action-question` 后也会约束回只读。
- 2.0.20 反例仍保持可执行：
  - `Please fix src/login.ts.`
  - `Can you run npm test?`

### 2.0.20 验收证据

- Focused 回归：`158/158 PASS`。
- `verify:packaged-bridge`：PASS。
- 完整顶级用户仿真：
  - 命令：`node scripts/devseek-top-agent-user-simulation-runner.mjs --run-id 20260814-intent-2.0.20-local-acceptance-g338fac0 --markdown docs/testing/devseek-20260814-intent-2.0.20-local-acceptance-g338fac0.md --force`
  - 结果：PASS。
  - Required acceptance cases：`49`。
  - Selected cases：`51`。
  - 新增维度：`advisory_action_question_boundary`。
  - Missing dimensions：None。
  - Execution evidence missing：None。
  - Fixpoint replay needed：false。
- 注意：`verify:current-candidate-identity` 曾因当前没有稳定 VS Code runtime 进程失败；包与安装身份已识别为 2.0.20，受控 VSIX 仿真不受影响。
- 证据 Markdown 当前为 untracked：`docs/testing/devseek-20260814-intent-2.0.20-local-acceptance-g338fac0.md`。如果下一轮需要保留远端证据，可作为文档提交单独处理；不要混入运行时代码改动，除非用户明确要求。

### 最新用户流程要求

- 同一任务迭代尽量只形成一个提交记录。
- 如果中途需要临时提交，后续用 amend/追加方式收敛到同一记录。
- 完整测试通过后再一次 push。
- 当前阶段不用打 tag。
- 每轮完成后如果用户要求暂停，就不要继续开下一轮，只更新交接文档和状态。

### 2026-08-14 新增策略：带噪自然语言语义恢复

用户明确要求 DevSeek 像 Codex/Claude Code 一样理解真实输入，不要求用户每个字、格式和语法都正确。必须覆盖中文错别字、同音字、口语、省略、ASR 风格断句、中英混输以及任务中途补充/纠正要求。

源码对标结论：

- Codex `protocol/src/user_input.rs:15` 保留原始 `UserInput::Text`，其 `text_elements` 也明确不修改 literal text。
- Codex `protocol/src/models.rs:1767` 将原始文本直接作为模型 `InputText`；自然语言理解由模型承担，不存在可见的“先通用纠错、再关键词定路由”核心流程。
- Codex `core/src/session/turn_input.rs:374`、`:546` 支持把新用户输入 steering 到活跃 turn，说明后续补充和改口属于同一任务的动态合同修订。
- Codex 的 tool router、exec policy、approval 和 sandbox 与模型理解分离：模型可以猜对意思，但不能自行获得写入、删除或外部 effect 权限。
- Claude Code 核心未开源，不能声称知道其内部算法；公开 CHANGELOG 可确认实时 steering、用户显式边界和权限分类器是分离的产品能力。详细证据见 `UPSTREAM-AGENT-SOURCE-AUDIT-20260813.md`。

策略修订为“原文保真 + 模型语义恢复 + 字面量 grounding + 本地权限仲裁 + 动态合同修订 + 证据闭环”：

1. 原始输入不可变保存。新增 `OriginalInputEnvelope` 概念，携带原文、附件、turn/session id、语言提示和 protected literal spans。
2. 模型输出升级为 `SemanticIntentProposalV2`，建议字段包括：`normalized_meaning`、`correction_hypotheses`、`intent_candidates`、`protected_literals`、`ambiguities`、`confidence`、`reasoning_evidence`。纠正结果是 proposal，不覆盖原文。
3. 模型负责恢复自然语言中的错别字、同音字、语序、省略和中英混输，例如把“请吧刚才问题修好，然候跑下测试”理解为“请把刚才问题修好，然后运行测试”。不要为这些长尾输入维护关键词穷举表。
4. 路径、命令、URL、版本、hash、代码标识符和引号内文本默认不得拼写纠正。模型提出的路径/命令候选必须经过 workspace 文件、符号索引、`package.json` scripts 等真实证据 grounding。
5. 纠错不能创造权限、删除否定含义或提升 effect：`不要该源代码` 可以理解为“不要改源代码”；但不能把咨询式删除纠正成授权删除，也不能把未知路径静默替换后执行。
6. 风险分级处理：只读分析可在高置信纠正后继续并披露关键假设；普通写入必须有目标 grounding；destructive/external effect 或否定词存在歧义时必须确认或澄清。
7. 用户中途输入不是新任务孤岛。后续输入可以补充、纠正、收窄、扩大或撤销当前要求，生成 contract revision，保留来源和优先级，最新明确约束优先。
8. 多语言/关键词配置只负责高置信本地事实和安全边界，不承担通用拼写纠正，也不直接决定最终 intent。

当前实现缺口：`packages/vscode-extension/src/app/semantic-intent-interpreter.ts` 会跳过带 `web` capability 的 provider，`SemanticIntentInterpretation v1` 也没有纠正假设、受保护字面量和多候选字段。因此，当前尚未完整利用真实 DeepSeek Web 大模型处理带噪输入。后续实现必须采用 provider-agnostic semantic proposal：独立解释调用和主 agent response 的结构化语义 envelope 都要进入同一个 `TaskSemanticContract` arbiter。

### 按 Codex 源码进一步修订：从“意图预分类”转向“动作级仲裁”

重新对照本地 `openai-codex` 源码后，当前策略还需修正：Codex 的核心优势不是单独运行一个更强的 intent classifier，而是把原始输入交给主模型，在同一 turn loop 中由模型直接回答、澄清或提出工具调用；本地层针对真实 action 做逐次权限与 sandbox 仲裁。

DevSeek 当前差距：

- `semantic-route-service.ts` 先执行本地 route，再可能额外调用一次 semantic interpreter，然后重新选 workflow，存在本地分类器与主模型“双重理解”。
- `semantic-intent-interpreter.ts` 对 Web Provider 直接跳过，因此真实 DeepSeek Web 路径仍主要受本地关键词 route 控制。
- `extension.ts:408` 用关键词正则提前决定 write request/context discovery；带噪输入可能在模型看到之前走错分支。
- `permission-service.ts` 根据预判 mode 生成工具 allow/deny 集合。该策略适合作为默认最小权限提示，但不能代替对模型实际 action 的 descriptor/effect/target/risk 仲裁。
- 好的基础已经存在：`AgentToolExecutor.plan()` 会把实际工具调用转换为 action descriptor，并计算 effect、risk、protected path 和 permission；canonical executor/authority receipt 可作为统一执行权威。

后续目标结构改为：

1. 原始输入和 steering 进入 turn-scoped input store，原文不被纠错结果覆盖。
2. 主 agent 模型读取原始输入、上下文和当前合同，直接输出回答、澄清或 action proposal；semantic proposal 是同一模型理解的附属结构，不是独立路由权威。
3. `TaskSemanticContract` 收敛为 constraint contract：保存 no-write/no-run、目标 scope、protected literals、approval mode、completion obligations 和 revision，不把 task kind 当作权限令牌。
4. 每个真实 action 在执行前统一经过 `ActionAuthorityArbiter`，根据 action descriptor、effects、target、risk、sandbox、最新 contract revision 决定 `allow/confirm/deny`。
5. 工具结果回灌模型，统一 evidence tracker 维护 net diff、readback、命令输出、测试和 external-effect receipt，再由 completion contract 判定结束。
6. 用户后续改口会产生新 revision；尚未执行的旧 authority receipt 立即失效，避免模型按过期意图继续写入或运行。

组件决策：

- 保留：`TaskSemanticContract` 中的确定性约束、`AgentToolExecutor`、canonical executor、authority receipt、文件/终端 permission coordinator 和 evidence store。
- 降级：`ChatIntentDecision.mode`、`SemanticIntentInterpretation.taskKind`、learned intent 只用于 UI、上下文准备和默认 workflow，不再直接授予执行权限。
- 重构：`resolveSemanticRouteDecision()` 优先消费主模型首轮的 semantic/action envelope；独立 semantic intent 调用只作为 provider 不支持结构化主响应时的辅助 fallback。
- 收敛：所有会影响权限、上下文正确性和完成判定的关键词快捷路径迁入 shared semantic/action owner。配置 lexicon 继续存在，但只提供高置信事实和安全 veto。

这使最终方案从“关键词提取 + LLM 分类 + 本地路由”进一步优化为“原始 turn 输入 + 主模型理解/动作提案 + 本地 constraint/action 仲裁 + 工具观察循环 + evidence 完成合同”，与 Codex 公开实现更加一致。

新增仿真要求：

- 建立 clean/noisy metamorphic pairs：规范输入与带噪同义输入必须得到等价的 task kind、mutation、effect 和 completion contract。
- 覆盖中文同音/错字：`吧/把`、`在/再`、`原/源`、`该/改`，以及乱序、少标点和口语表达。
- 覆盖 advisory/direct 对偶：`应该删出 dist/cache 吗` 必须只读；`请删出 dist/cache` 必须进入 destructive confirmation。
- 覆盖外部 effect：`吧这些改动提交并推送` 应恢复为 commit/push 请求并进入确认。
- 覆盖 literal protection：`src/logim.ts`、`npm tset` 只能在 workspace/script 证据支持时建立候选映射，不能静默改写执行。
- 覆盖否定与权限反例：带噪的“不要修改”“先别运行”“不要 push”必须保持 veto；纠错不得把 advice 变成 authorization。
- 覆盖多轮 steering：先要求修复，随后说“刚才说错了，只分析，不要改”，最终合同必须撤销写权限。
- 在 top-agent runner 增加 required case `noisy-multilingual-intent-recovery`；与 `destructive-external-advice-boundary` 一起完成时，required case 应由 `49` 增至 `51`。

### 下一轮优先缺口（建议 2.0.21）

2.0.20 后的探针显示，普通 edit/run advisory 已修复，但 destructive/external-effect advice 仍存在 Codex/Claude Code 对标缺口：

| 探针输入 | 当前错误行为 | 期望行为 |
| --- | --- | --- |
| `Should I delete dist/cache?` | 路由为 `destructive`，进入确认/可执行边界 | 只读建议/风险分析，不请求删除 |
| `Should we remove src/legacy.ts or keep it?` | 路由为 `destructive`，带 `src/legacy.ts` mutation target | 只读比较/建议，不请求源码删除 |
| `What would happen if I delete dist/cache?` | 路由为 `destructive` | 只读影响分析 |
| `Can you explain whether we should delete dist/cache?` | 路由为 `destructive` | 只读解释/建议 |
| `Please commit and push these changes.` | 当前被路由为 `existing-project-edit`，未进入 external-effect confirmation | 应识别为外部 effect，请求确认边界，不能当作源码编辑 |

建议实现方向：

1. 先建立 Codex-style turn/action 边界：原文保真、主模型语义/action envelope、constraint contract 和 action-level authority；避免通过继续堆叠 typo 或同音字关键词解决新样例。
2. 将 advisory-action-question 的语义范围扩展到 destructive/external-effect advice，但不能误杀 `Please delete ...`、`Delete ... now`、`Please commit and push ...` 等真实授权。
3. 在 destructive 判断前增加“咨询式破坏性动作问题”剥离，保证 `Should I delete...` 不生成 mutation target 或 destructive kind。
4. 在 external-effect owner 中补齐直接 `commit/push/deploy/release` 授权识别，确保真实外部 effect 请求进入 confirmation，而不是 existing-project-edit。
5. 继续采用“模型语义理解 + 本地契约仲裁 + 证据闭环”：
   - 模型可提出 destructive/external-effect 候选；
   - 本地 advisory 边界必须能拒绝咨询式候选；
   - 真正授权的 destructive/external-effect 仍 fail-closed 到确认。
6. 新增测试：
   - `task-semantic-contract.test.mjs`：destructive/external-effect advice 不请求 mutation/external effect；direct destructive/effect 仍保持 confirmation。
   - `task-intent-router.test.mjs`：上述探针应为 chat/read-only 或 external-effect confirmation。
   - `chat-controller.test.mjs`：模型误给 destructive/run/external-effect proposal 时，本地 advisory 边界仍压回只读。
   - `semantic-intent-interpreter`/arbiter tests：clean/noisy 等价、原文保真、literal grounding、否定/权限反例和多轮 revision。
   - `scripts/devseek-top-agent-user-simulation-runner.mjs`：新增 `destructive-external-advice-boundary` 和 `noisy-multilingual-intent-recovery`，使 acceptance required case count 从 `49` 增至 `51`。

### 推荐下一轮命令节奏

1. 先跑探针复现，不要立即改大范围代码。
2. 修改 semantic owner 后跑 focused：
   - `node --test packages/vscode-extension/test/unit/task-semantic-contract.test.mjs`
   - `node --test packages/vscode-extension/test/unit/task-intent-router.test.mjs`
   - `node --test packages/vscode-extension/test/unit/chat-controller.test.mjs`
   - `node --test scripts/test/devseek-top-agent-user-simulation-runner.test.mjs`
3. 合并 focused：
   - `node --test packages/vscode-extension/test/unit/task-semantic-contract.test.mjs packages/vscode-extension/test/unit/task-intent-router.test.mjs packages/vscode-extension/test/unit/chat-controller.test.mjs scripts/test/devseek-top-agent-user-simulation-runner.test.mjs`
4. bump 到 `2.0.21`，focused 全绿后本地单提交，暂不 push。
5. release loop：
   - `npm run extension:package:debug`
   - `code --install-extension /home/ff/work/devseek_netai/devseek-netai-latest.vsix --force`
   - `npm run verify:packaged-bridge`
6. 完整 acceptance：
   - `node scripts/devseek-top-agent-user-simulation-runner.mjs --run-id 20260814-intent-2.0.21-local-acceptance-g<commit> --markdown docs/testing/devseek-20260814-intent-2.0.21-local-acceptance-g<commit>.md --force`
7. 验收通过后，一次 push；不打 tag。

## 卡顿处理结论

当前窗口不建议继续长时间迭代。

已检查系统状态：

- 未发现仍在运行的 `devseek-real-plugin-deepseek`、Playwright 或 Chromium 自动测试进程。
- 系统内存仍充足，负载不高。
- VS Code 当前处于软件 GPU/SwiftShader 渲染，并出现 GPU stall 日志。
- 当前 DevSeek 工作区窗口和另一个 `uav` 工作区窗口都挂着 Codex app-server；卡顿主要来自 VS Code/Electron renderer/zygote，而不是 DevSeek 自动测试残留。

建议：

1. 用新对话继续本交接文档里的任务。
2. 当前对话保留到确认文档可读即可，之后重载或关闭当前 VS Code 窗口。
3. 如果还卡，关闭不需要的 `uav` VS Code 窗口，再执行 `Developer: Reload Window`。
4. 下一轮不要立即跑完整 headed 大测试；先修 `destructive/external-effect advice` 语义边界，再跑 focused 回归和完整顶级用户仿真。

## 2026-08-13 历史已发现问题

| 编号 | 问题 | 根因判断 | 当前状态 |
| --- | --- | --- | --- |
| P1 | 用户要求生成 Markdown 报告，但 DevSeek 投影成 review/no-workspace-mutation，拒绝写文件 | 报告类产物没有被 semantic contract 视为受控 workspace artifact | 已修复，已补 acceptance 测试 |
| P2 | 用户说“不要修改源码”，DevSeek 把它当成全局禁止写入，导致报告文件也不能创建 | write authority 没有区分 source-only 约束和 report-only artifact 写入 | 已修复，已补 write-authority 单测 |
| P3 | 文本中的“登录/ready 边界”被当成 `/ready` 绝对路径，导致重复寻找不存在目标 | 路径抽取器把自然语言斜杠词误判为文件路径 | 已修复，已补 router 回归测试 |
| P4 | 真实 UI 输入不流畅，VS Code Webview DOM 不稳定，等待 15 秒后才走坐标输入 | 自动测试过度等待 DOM 探测；实际应快速降级到屏幕坐标输入 | 已把默认 DOM 探测等待降到 2 秒，待语法检查和真实 UI 复测 |
| P5 | 报告文件已经生成且锚点齐全后，DevSeek 仍进入验证/完成信号不匹配，之后反复执行只读命令 | completion evidence 与 auto-validation 仍按代码任务思维要求运行/验证信号，没有为 report-only artifact 建立独立完成合同 | 历史入口；后续迭代已完成多轮覆盖。除非新验收复现，不要再把 P5 当当前第一优先级 |

新对话继续时，以本文最前面的最新版本结论和“当前停止点”为准；以下内容仅保留历史问题来源。

## 当前工作区状态

`2.0.24` 本轮要求一个最终提交并一次推送。新对话不要依赖文档中的旧 hash，必须重新执行：

```bash
git status --branch --short
git log --oneline -5
git ls-remote origin refs/heads/devseek-multi
```

已知未跟踪文件主要是历史或本地生成的 `docs/testing/*.md` 验收摘要；不要批量加入 Git。2.0.24 精选且应提交的文档是：

- 本 handoff
- `docs/top-agent-convergence-audit-20260711/UPSTREAM-AGENT-SOURCE-AUDIT-20260813.md`
- `docs/top-agent-convergence-audit-20260711/T5-MEMORY-CODEX-ALIGNMENT-ARCHITECTURE-IMPLEMENTATION-20260817.md`
- `docs/top-agent-convergence-audit-20260711/MEMORY-ENGINEERING-DECISION-RATIONALE-20260817.md`
- `docs/top-agent-convergence-audit-20260711/PLAN-当前收敛迭代计划.md`
- `docs/testing/devseek-20260817-t1-t5-medium-program-2.0.24-focused-r5.md`
- `docs/testing/devseek-20260817-t1-t5-memory-session-complete-2.0.24-final.md`

历史本地摘要示例：

- `docs/testing/devseek-20260813-intent-2.0.0-tagged-local-acceptance.md`
- `docs/testing/devseek-20260813-intent-2.0.1-tagged-local-acceptance.md`
- `docs/testing/devseek-20260814-intent-2.0.18-local-acceptance-gadcc7a5-focused-rerun.md`
- `docs/testing/devseek-20260814-intent-2.0.18-local-acceptance-gadcc7a5.md`
- `docs/testing/devseek-20260814-intent-2.0.20-local-acceptance-g338fac0.md`
- `docs/testing/devseek-intent-semantic-arbiter-2.0.0-20260813-local-acceptance.md`
- 不要把历史 `docs/testing` 证据文件误混入运行时代码提交。

## 意图识别对策

当前不能继续依赖“关键词命中即判定”的方式。对标 Codex/Claude Code 的可观察行为，DevSeek 应采用“模型语义理解 + 本地契约仲裁 + 证据闭环”的分层方案。

推荐架构：

1. **确定性提取层**
   - 只提取高置信事实：显式路径、文件类型、禁止项、用户指定命令、产物路径、是否要求测试。
   - 不直接决定最终任务类型。
   - 对自然语言中的斜杠词、版本号、URL、命令片段做上下文过滤。

2. **LLM 语义候选层**
   - 让模型把用户输入解析成结构化候选，不让模型直接授权写入。
   - 输出字段建议包括：`intent`、`mutation_kind`、`deliverable_kind`、`target_paths`、`constraints`、`verification_need`、`ambiguities`、`confidence`、`reasoning_evidence`。
   - 允许多候选，例如“分析并生成报告”同时包含 read/analyze/report artifact，不应被折叠成 pure review。

3. **Canonical Task Contract 仲裁层**
   - 由本地 shared owner 融合确定性提取、LLM 候选、workspace 状态、session 记忆和实时用户 steering。
   - 写入、运行、外部 effect 必须由本地合同裁决。
   - 规则要按作用域解释：`不要修改源码` 不等于 `不要创建报告文件`；`不要创建任何文件` 才是全局写入撤销。

4. **执行与完成证据层**
   - 每种任务建立自己的完成合同。
   - 代码修改任务需要 diff、测试/编译或明确跳过原因。
   - 报告类任务需要文件写盘、readback、关键锚点/章节/用户要求覆盖，不应强制终端验证。
   - provider 没有显式 `task_complete` 时，如果本地证据已经满足合同，应允许 agentic loop 形成受控完成总结，避免继续循环调用工具。

5. **低置信澄清层**
   - 当用户输入确实含糊，例如“处理一下这个问题”，DevSeek 应问一个短问题。
   - 当用户给出可执行目标和边界时，不应反复追问。

## DeepSeek Web 兼容策略

真实入口是免费 DeepSeek 网页，必须兼容网页回复不稳定：

- DeepSeek 可能返回混合自然语言、Markdown、代码块、JSON-like 工具调用、半截工具调用。
- 解析器要容错，但不能无限重试。
- prompt 应要求模型用稳定工具协议返回；本地仍必须能处理偏离协议的回复。
- 工具调用失败后要有有界重试和错误分类；相同失败不能反复执行。
- 当报告文件已经满足合同，不应因为 provider 又输出 `ls/head/grep` 之类只读命令而重新进入大循环。
- Debug 版本测试必须记录 DeepSeek 网页原始反馈摘要、解析结果、工具执行、合同状态、缺失证据和终止原因。

## 历史 P5 修复背景

P5 曾是 2026-08-13 的第一优先级：report-only Markdown artifact 的完成/验证合同。后续 2.0.x 迭代已围绕该类问题完成多轮修复与验收；除非新的用户仿真重新复现，不要把本段当作当前下一轮入口。

目标行为：

- 用户要求“基于源码分析并生成 Markdown 报告，不修改源码”时，DevSeek 应允许写指定报告文件。
- 创建报告后应做 workspace readback 和内容锚点检查。
- 如果没有代码修改、没有运行要求、用户还禁止改源码，就不应强制走代码任务的 compile/test/run auto-validation。
- 若本地证据已经满足 deliverable contract，即使 DeepSeek 没有明确发 `task_complete`，也应生成受控完成总结并结束。
- 同类问题要修在 semantic/completion owner，不要在单个测试 prompt 上打补丁。

历史相关文件：

- `packages/vscode-extension/src/agent/agentic-loop.ts`
- `packages/vscode-extension/src/agent/auto-validation.ts`
- `packages/vscode-extension/src/agent/completion-evidence.ts`
- `packages/vscode-extension/test/unit/markdown-deliverable-flow.test.mjs`

历史建议测试方向：

- report-only Markdown 创建成功且 readback 匹配时，不触发终端 auto-validation。
- provider 只创建文件、没有显式 `task_complete`，但合同证据满足时，agentic loop 可以完成。
- report-only artifact 中的 `不要修改源码` 不撤销报告写入。
- 文本中 `登录/ready 边界` 不产生 `/ready` 目标；显式“读取 /ready 文件”仍识别为路径。

## 后续专题迭代计划

| 专题 | 目的 | 真实用户仿真 case |
| --- | --- | --- |
| T1 意图识别 | 验证单轮、多轮、连续 steering 下的任务类型、写入范围、产物类型是否正确 | 报告生成、代码修复、只读 review、模糊需求澄清、路径/斜杠文本 |
| T2 执行流程 | 验证计划、编辑、验证、修复、总结是否符合 coding agent 行为 | 小型 bug 修复、测试失败后修复、报告 artifact、用户中途改约束 |
| T3 DeepSeek Web 兼容 | 收集真实网页返回的偏离格式并增强解析/提示词 | 工具 JSON 混杂 Markdown、半截工具调用、重复命令、错误后恢复 |
| T4 权限与写入边界（已完成） | 减少无害 workspace 新文件的误拦截，同时保持外部 effect fail-closed | 指定目录生成文件、源码只读报告、拒绝外部目录、拒绝 shell 危险命令 |
| T5 记忆与重启 | 验证 VS Code 重启后是否保留必要上下文且不污染新任务 | 记录用户项目偏好、重启后继续、跨 session 不误用旧任务 |
| T6 卡顿与资源回收 | 保证自动测试窗口、浏览器、bridge、日志能按策略回收 | 每轮开始检查残留；保留最后窗口；新测试前关闭上次窗口 |
| T7 类型与架构债务（已完成当前专题） | 让类型错误阻断发布，并按 semantic owner 清理重复实现和证据链缺口 | 严格 typecheck、授权输入替换、跨层 contract、架构漂移 |
| T8 效率对标（已完成当前专题） | 隔离无关上下文，有界并发安全观察，不破坏 effect 顺序 | 内部日志搜索污染、四路只读观察、mixed 读写、exact-VSIX 完整流程 |

## 真实用户仿真测试要求

必须坚持真实用户输入方式：

- 测试输入必须进入 VS Code DevSeek 窗口，不能只用单体模拟程序替代。
- 日志必须能证明输入方式、DeepSeek 网页反馈、解析结果、工具调用、文件结果和完成状态。
- 每个 case 的 Markdown 记录必须包含：用户输入、期望意图、期望约束、期望产物、实际日志证据、问题分析、修复说明、复测结果。
- 一个 case 通过后不要重复跑，除非代码修改影响该路径。
- 修复后先跑小型 targeted case，再跑大 case，避免每次从零开始耗时循环。

## 仿真用户测试计划

仿真测试不是单体测试替代品，而是最终验收入口。单体测试只用于锁定修复点和防回归；真正判断 DevSeek 是否接近顶级编程智能体，必须看 VS Code DevSeek 窗口中输入真实用户需求后的完整执行链路。

测试组织方式：

1. 每个专题建立一个 Markdown case 记录，放在本专题目录或本专题子目录中。
2. 每个 case 必须有唯一编号，避免重复跑已经通过且未受代码修改影响的场景。
3. 每轮修复先跑最小 targeted case，确认修复点，再跑覆盖真实用户需求的大 case。
4. 大 case 只在关键路径变更后运行，不能每发现一个小问题就从零开始完整重跑。
5. 每次真实 UI 仿真前检查残留进程；如果要再次测试，先关闭上一次自动测试窗口，但最终保留最后一次测试窗口供人工确认。
6. 每轮结束清理无用日志、临时工作区和旧测试产物，避免占用硬盘。

Case 记录模板：

```markdown
## CASE-ID 标题

- 用户输入：
- 测试入口：VS Code DevSeek 窗口真实输入 / DeepSeek Web Provider
- 期望意图识别：
- 期望写入/运行/权限边界：
- 期望产物：
- 期望完成证据：
- 实际执行摘要：
- 关键日志：
- DeepSeek 网页反馈摘要：
- 工具调用与结果：
- 问题分析：
- 修复说明：
- targeted 复测：
- 大 case 复测：
- 结论：
```

首批建议 case：

| Case | 用户真实需求 | 覆盖点 | 通过条件 |
| --- | --- | --- | --- |
| SIM-INTENT-01 | “请阅读当前项目登录/ready 相关代码，在 `docs/r3-iteration/*.md` 生成分析报告，不修改任何源码。” | report-only artifact、source-only no-write、斜杠自然语言、完成合同 | 识别为报告产物写入；不修改源码；不误识别 `/ready`；报告写盘/readback/锚点通过；不进入重复终端验证 |
| SIM-INTENT-02 | “这个测试失败了，请定位并修复，必要时重构，然后跑相关测试。” | 代码修改、测试失败诊断、修复后验证 | 先定位再改代码；改动有架构理由；相关测试通过；失败时给出下一步 |
| SIM-INTENT-03 | “先只 review 这段实现，不要改文件。” | 只读 review、写入撤销 | 不创建/修改文件；输出问题列表和证据 |
| SIM-INTENT-04 | “在指定目录新建一个脚本/配置文件，不要影响其他文件。” | workspace 新文件权限、低风险写入 | 只在指定目录创建目标文件；readback 匹配；无额外修改 |
| SIM-INTENT-05 | “上一轮结论继续，但现在不要跑大测试，只验证刚才修复点。” | 多轮 steering、避免重复大测试 | 识别为 targeted verification；不从零跑完整链路 |
| SIM-INTENT-06 | VS Code 重启后继续同一专题 | 记忆、session 恢复 | 能找到上轮文档和未完成任务；不把历史测试当作当前通过 |

日志验收最小字段：

- `natural-ui-submitted`：证明不是命令注入，而是真实 UI 输入。
- DeepSeek 原始反馈摘要：证明网页返回被记录。
- 解析结果：意图、产物、权限、约束、验证需求。
- 工具调用：文件写入、读取、终端命令或明确未运行原因。
- 合同状态：缺失证据、满足证据、完成/失败原因。
- 产物路径和 readback 摘要。

## 迭代过程硬性要求

这些规则来自本轮反复交互，下一轮必须继续遵守：

1. **目标口径**
   - 当前只能说“向顶级编程智能体收敛”，不能因为本地测试通过就声明正式达到顶级资格。
   - 顶级资格必须等待真实 Provider、正式项目、受保护 RC、盲 holdout 和发布决策全部通过。

2. **真实用户优先**
   - 以用户实际会怎么向 DevSeek 提需求为测试输入，不用过度人工构造的单体 prompt 冒充真实使用。
   - 单体测试已经覆盖过的内容不重复做，除非代码修改影响该路径。

3. **小 case 先收敛，大 case 后验收**
   - 出现问题后，先设计专门针对修复点的小型真实仿真 case。
   - case 失败时先分类产品缺陷、测试基础设施缺陷、oracle 错误或测试假设不成立；不得直接修改产品去迎合 case。
   - 关键 case 必须声明它要击穿的风险边界，并用历史缺陷回放、故障注入或 mutation 证明自身有检错能力。
   - 小 case 通过后再跑大 case，避免每轮都从零开始，最后仍卡在同一个问题。

4. **资源与窗口管理**
   - 每隔一段时间，尤其是 headed 测试前后，检查 VS Code、Playwright、Chromium、DevSeek 测试进程、CPU 和内存。
   - 无用自动测试进程要回收；不要强杀用户当前主 VS Code 会话。
   - 保留最后一次自动测试窗口供用户查看；再次测试前关闭上一次自动测试窗口。

5. **DeepSeek Web 调试**
   - Debug 测试必须记录网页反馈、解析结果和失败分类。
   - 重复失败要有有界重试和停止条件；同一错误不能无限循环。
   - 兼容 DeepSeek 返回自然语言、Markdown、JSON-like、半截工具调用和额外解释。

6. **提交要求**
   - 提交前必须测试通过。
   - 小修复不要频繁提交；相关问题收敛、release loop 通过、真实 UI 仿真有证据后一次性提交。
   - 未测试通过时只能保留工作区修改和报告，不提交。

7. **文档要求**
   - 每轮真实仿真都要形成 MD 报告。
   - 报告必须包含用户输入、期望结果、实际执行日志证据、问题分析、修复、复测结论。
   - 用不上的测试日志和临时产物要清理。

## 代码修改原则

DevSeek 当前处于开发阶段，未发布；旧逻辑不需要时可以直接删除，避免为了兼容旧实现污染新架构。

必须遵守：

- 修复缺陷类别，不只修当前 prompt、当前日志或当前截图。
- 先找 semantic owner，再改代码；不要在多个入口堆重复判断。
- 必要时重构，让行为契约、权限、执行、验证、完成证据各自有清晰边界。
- 对标 Codex/Claude Code 的可观察优秀行为，但不要简单照搬公开文档；应转化成 DevSeek 可验证的本地契约。
- 不能只靠关键词做最终意图识别；关键词只能作为确定性事实来源之一。
- 写入权限要按作用域判断：指定 workspace 目录中新生成文件通常是低风险写入，但外部目录、源码修改、命令执行和外部 effect 仍需严格控制。
- “不要修改源码”是 source-only 约束，不应阻止用户明确要求的 Markdown 报告写入。
- “不要创建任何文件/不要改任何文件”才是全局写入撤销。
- 报告类任务不应强制走代码编译/运行验证；应使用文件 readback、章节/锚点覆盖、用户要求覆盖作为完成证据。
- 修改 extension 或 bridge 行为后，必须执行默认本地 release loop：compile、package VSIX、install latest VSIX。
- 代码、测试和文档都要保持可维护；不要为了缩短文件而压缩格式、删除必要说明或制造空壳兼容层。

## 2026-08-24 N4 授权真实 Provider canary 与恢复契约收敛

- 授权边界：用户已批准 `R4-LIVE-AUTH-01/02` 范围内的真实 DeepSeek Web、headed VS Code 和本地 clean-runtime 测试。`R4-LIVE-AUTH-03/04/05`、受保护 profile、trusted external evidence 与 qualification import 仍缺失；本记录的 qualification effect 为 `NONE`，Gate 0 仍为 `NOT_PASSED`。
- 根因类别：首轮 canary 保留了正确产物，但安全重试因 checkpoint 重建任务身份而立即拒绝；后续还暴露出裸 `Action:` 文本被误当终端回答、只读上下文被误归为交付物，以及完成证据未在协议恢复前优先结算。
- 责任边界修复：`4cd7c38` 将 checkpoint 升级为绑定原始 canonical task contract 的 v3 协议，恢复时复用不可变任务身份；`7d7871c` 由唯一协议检查 owner 隔离未进入授权 envelope 的工具形文本，只能发起当前 envelope 的有界重试；`6609ea0` 分离 observed context 与 deliverable target，并让 transport 和 in-band protocol failure 共用“先结算已完成证据、后恢复”的路径。旧 Provider recovery 参数、双轨分支和专属死路径已删除。
- Codex 源码事实：主基线为 `code/upstream-agent-sources/openai-codex` @ `fe614a6304ef804be74a622e482fdd75977abcba`；`codex-rs/core/src/session/turn_context.rs` 与 `rollout_reconstruction.rs` 保留并重建 turn context，`session/turn.rs` 与 `stream_events_utils.rs` 仅将结构化 `FunctionCall`/`CustomToolCall` 交给 ToolRouter，普通 message 不获得工具权限。DevSeek 按责任映射这些边界，不照搬 Rust 结构。
- Claude 证据等级：辅助对照归档 @ `be90077c6a353f292fa612d97173865a9ab21b83` 的公开文档、hooks、权限与可观察行为；其核心 agent loop 不可公开审计，本轮不宣称掌握闭源内部实施。
- 回归证据：Shared `366/366`，Bridge `42/42`，CLI `84/84`，Headless `25/25`，Extension `174/174 suites`，进一步聚焦 `197/197`；`Phase 10` 的编译、类型检查、全套件与 25/25 用户旅程通过，architecture drift 为 0 violation。
- 发布循环：`devseek-netai-latest.vsix` 及两份精确副本 SHA-256 均为 `858f74183eabeed60e8d912cfa76c2be6cab6285d6153a1b67340ad82de69d08`，版本 `2.0.32-debug.20260824.t153259.g6609ea0`，source `6609ea07afce643afc1a4d2f3c5d7266c6bc4909`；包内 Bridge SHA-256 为 `8add0a5912cd4c70ed107a459b32881af8b1086ee18e0083a90058dcd93a5ef7`，HTTP 健康检查、强制安装和唯一 stable Bridge 探针均通过。
- 真实用户入口结果：headed VS Code Webview 屏幕坐标点击与键盘输入，`naturalUi=true`、`commandInjected=false`；真实 DeepSeek Web 运行 1/1 PASS，8 次 Provider request 全部终结，8 次工具执行，1 次 committed mutation，`tasksApplied=1`、`tasksFailed=0`、in-flight=0。目标 Markdown 13,014 bytes、231 行、20 个 heading、10/10 验收锚点存在，run-log replay 返回 0。
- 独立性边界：上述 1/1 是实现者本地授权 canary，不计入 N3 外部独立平台结果，不替代 Windows/WSL/macOS 主机、sealed holdout 或发布裁决。

## 2026-08-24 N4 最终候选 Provider authority 与原始 turn 收敛

- 最终候选：source `2db5768a70ebd53aea6c279328e2c87a9ad1aab2`，VSIX `devseek-netai-2.0.32-debug.20260824.t170937.g2db5768.vsix`，SHA-256 `236d45d9ad7559e82912435e3f47bd0633e4259617e8bb2cda235a1b96875951`，包内 Bridge SHA-256 `8add0a5912cd4c70ed107a459b32881af8b1086ee18e0083a90058dcd93a5ef7`。compile、package、Bridge HTTP 校验、强制安装和当前身份 11/11 均通过，最终只有一个 stable exact-candidate Bridge。
- 新真实失败：C13 自然输入中的 `@modelcontextprotocol/sdk` 被 UI file mention 层改写成“找不到文件”，破坏原始 turn；Provider 的无效或未完成工具 envelope 可退化为普通完成文本；只有 read-only evidence 时也可能错误结算未完成动作；恢复后 prose-only 重试还可能假完成。另一轮发现 Provider 在精确 channel opening 后使用传统 `</devseek_tool_calls>` closing，旧 parser 无法在已认证上下文中接受。
- 责任收敛：file mention owner 只为成功解析的文件注入内容，未解析 token 保留原始输入；text-tool protocol owner 在精确当前 channel opening 后接受受限传统 close，但继续拒绝错误 opening channel；agentic loop 统一 invalid/incomplete/out-of-envelope 恢复并持有 unresolved protocol state；settlement owner 禁止只读证据结算工具协议失败，真实已提交写入仍可在恢复前结算。旧的 UI 文本改写和可绕过恢复分支已删除。
- Codex 事实基线仍为 `fe614a6304ef804be74a622e482fdd75977abcba`：`turn_context.rs`、`rollout_reconstruction.rs` 负责保留/重建 turn context，`session/turn.rs`、`stream_events_utils.rs` 只给结构化 tool call 动作权限。Claude Code 仅使用公开归档 `be90077c6a353f292fa612d97173865a9ab21b83` 的文档、hooks、权限和可观察行为作辅助，不推断闭源 agent loop。
- 回归：Extension 全量 `175/175 suites`，本专题聚焦/攻击回归 `208/208`，TypeScript compile 与 `git diff --check` 通过。发布制品由最终代码提交生成并安装。
- 最终 exact-VSIX 前台 wave 3/3 PASS：C13 为 6/6 Provider terminal、8 个 raw tool event、1 个 committed mutation、14,075 bytes/295 行/49 headings、12/12 锚点、replay=0；R3-08A 为 6/6 terminal、6 tools、1 mutation、15,658 bytes/284 行/30 headings、replay=0；R3-09A 为 10/10 terminal、12 tools、1 mutation、8,799 bytes/159 行/10 headings、replay=0。三项均为 headed VS Code、`naturalUi=true`、`commandInjected=false`。
- 可靠性告警：R3-09A 约 270 秒才形成成功快照，前台 in-flight=0 时还有 2 个无工具、无 mutation 的后台记忆请求未终止；旧候选 C11 也在前台通过后因同类后台维护跨越 300 秒报告窗口。因此本轮只声明“前台 3/3 通过”，不声明全波次 full-idle 或后台时延达标。
- 外部边界：上述是用户授权的本地开发波次，不计入 N3 独立跨平台结果，也不导入 formal live qualification。`R4-LIVE-AUTH-03/04/05`、trusted external evidence、sealed holdout 和 Gate 0 仍保持阻塞。

## 当前停止点

- `2.0.32` 已完成 T12 模型主导语义、逐动作仲裁和回执验收，并由 `2db5768` 继续收敛原始 turn、Provider 协议 authority 与恢复结算；T1-T11 已完成的 DeepSeek Web、权限、记忆、动态修订、直接回答、真实代码执行、待确认编辑、生命周期、类型、Provider 证据和效率闭环保持有效。
- 本轮按同一任务一个提交、测试全通过后一次 push、不打 tag 收口；本文件所在最终提交就是 T12 本轮记录。以 `git log -1` 和远端分支为准；不要把受控 VSIX 与本地仿真通过外推为 C14、真实模型质量或全行业同级认证。
- 新对话先检查 `git status --branch --short`、`git log -1 --oneline`、`git ls-remote origin refs/heads/devseek-multi`、`sha256sum devseek-netai-latest.vsix` 和本地 VSIX 安装列表，确认最终提交、远端分支和安装包一致。
- 当前本地代码专题已收敛；后续先接收 `code/devseek-tests/n3-cross-platform-user-way/` 的 Windows、WSL2、macOS 独立证据。任何 FAIL 必须保留第一次现场并回流 defect class；发布级验收仍需受保护 RC、sealed holdout 和 C14 外部证据门禁。

## 新对话启动提示

可以在新窗口直接输入：

```text
请读取仓库根 AGENTS.md、docs/top-agent-convergence-audit-20260711/PLAN-当前收敛迭代计划.md 和本交接文档。当前本地产品候选 source 为 devseek-multi/2db5768，exact VSIX 为 2.0.32-debug.20260824.t170937.g2db5768。N1/N2 已完成；用户授权的 N4 本地 headed natural-UI 前台 wave 3/3 通过，但 N3 独立跨平台结果仍为 0，R3-09A 只证明前台完成而未证明后台 full-idle。下一步先验证人工测试包回传，不要把 debug VSIX 当 protected RC，不要执行未授权 sealed holdout，也不要伪造 external authority 或 Gate 0 claim。所有回复中文。
```
