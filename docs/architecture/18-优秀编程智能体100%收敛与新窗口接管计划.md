# DevSeek 优秀编程智能体 100% 收敛与新窗口接管计划

文档编号：ARCH-18

最后更新：2026-07-11

状态：新窗口接管与后续迭代的唯一执行入口

功能代码基线：`e78c253`（与 rebase 前真实测试使用的 `06894bb` 文件树等价；接管时以当前 `HEAD` 为准）

开发分支：`devseek-multi`

关联文档：

- [05-代码重构实施计划.md](05-代码重构实施计划.md)
- [06-竞品实现方式对标审计与设计修正.md](06-竞品实现方式对标审计与设计修正.md)
- [14-自动闭环迭代与测试方法论检讨.md](14-自动闭环迭代与测试方法论检讨.md)
- [17-顶层RunContext与执行事实治理专题设计.md](17-顶层RunContext与执行事实治理专题设计.md)
- [TOP_AGENT_CHANGE_GATE.md](../process/TOP_AGENT_CHANGE_GATE.md)

## 0. 新窗口启动指令

在新窗口中直接发送下面这段指令，不需要重新复述历史聊天：

```text
请接管 /home/ff/work/devseek_netai 的 DevSeek 迭代。

开始前必须完整读取：
1. docs/architecture/18-优秀编程智能体100%收敛与新窗口接管计划.md
2. docs/architecture/05-代码重构实施计划.md
3. docs/process/TOP_AGENT_CHANGE_GATE.md

以当前 HEAD 为接管点，先核对 git status、最近提交、现有测试与本文记录的最新真实失败证据。
严格执行 05 实施原则：修改前先对标 Claude Code/Codex；修复缺陷类别和架构边界，不针对截图或维保任务硬编码；先建立 replay，再修改；unit -> replay -> 仿真 -> 真实插件逐级验证；完整 benchmark 通过后小步提交。

第一优先级不是再次重跑正式任务，而是完成本文 P0-A Evidence Grounding 和 P0-B 通用 TaskContract/QualityPolicy。把 20260711-131537 的错误事实产物固化为 replay，确保源码事实未经 EvidenceRef 验证不能进入交付物或完成结算。

每轮都要审计成果物质量、运行日志、Replay、UI 终态和 Git/VSIX 版本。没有真实证据不得宣称稳定。相同失败类别连续出现两次时停止重跑，回到架构边界重新设计。
```

## 1. “100% 优秀”的可验证定义

“100%”不是声称能够解决世界上所有未知编程任务，而是指 DevSeek 在约定发布范围内的所有硬性资格门禁均通过，不存在已知 P0 缺陷，也不通过假成功、弱 oracle 或人工挑选日志提高分数。

达到 100% 发布资格必须同时满足：

1. **任务理解正确**：正确识别既有工程实现、独立项目、只读分析、文档交付、验证修复、续作和破坏性操作；约束只作用于对应对象，不能把“不要修改源码”扩大成“不要创建报告”。
2. **事实完全可追溯**：源代码数值、接口、依赖、命令结果和生成文件均引用可审计 `EvidenceRef`；最终产物中的强事实与证据逐项一致，禁止臆造。
3. **软件工程闭环完整**：调查、需求澄清、设计、实现、验证、审阅、交付按任务需要执行；不固定文档数量，也不为简单任务制造无关文档。
4. **状态结算唯一且诚实**：所有入口统一经过 Agent 状态机；任何中途 Provider、工具、写盘、验证或 Replay 失败都保留为失败事实，不能被后续文本覆盖。
5. **工具和副作用可靠**：文本工具、native function calling、MCP 都归一为 `ToolCall`；写盘、终端和 MCP 副作用具备 checkpoint、幂等与恢复边界。
6. **验证与用户要求一致**：自动选择 build/test/lint/run/read-back；无法自动验证时进入 `waiting_user` 或 `manual_review_required`，不能伪造通过。
7. **Provider 可替换**：DeepSeek Web 只是 Provider Adapter；Headless Agent Core、工具、证据、状态、验证和 UI 结算不依赖网页 DOM 或特定模型话术。
8. **用户体验清楚**：用户看到连续保留的阶段总结；具体读文件、搜索和命令折叠展示；能明确知道当前完成了什么、为什么阻塞、下一步是什么。
9. **架构债务收敛**：职责持续从超大编排文件外移，架构预算只能下降；迁移完成后删除不合理或无用旧代码，不保留无业务价值的兼容分支。
10. **真实资格达标**：同一干净提交通过本文第 8 节规定的确定性、仿真、真实任务和竞品对标门禁。

任何一项硬门禁失败，完成度都不能记为 100%。

## 2. 当前基线与诚实进度

### 2.1 已形成的能力

截至功能代码提交 `e78c253`（rebase 前等价提交为 `06894bb`），已经建立：

- Provider 响应完整性检查、工具意图解析、continuation 合并和 XML + CDATA 无损多行写盘。
- `TaskHistory + Checkpoint + IdempotencyGuard` 的基础结构和 Runtime Replay。
- Git/VSIX/Bridge 运行版本绑定，旧服务不能证明新提交。
- 确定性 Phase 0-12、PA0-PA13、架构预算和稳定性资格报告。
- 失败保真、Todo/QualityGate 阻塞和部分 UI 阶段总结。
- 混合动作路由：读取既有源码、写新报告、禁止修改源码能够进入 `edit-agent`。
- 稳定资格不再由单元测试冒充，dirty worktree 和旧真实报告不能提升稳定等级。

### 2.2 最新真实 canary 结论

真实链路：VS Code 插件 -> 已安装精确 VSIX -> Bridge -> DeepSeek Web -> Agent 工具 -> 工作区文件。

运行版本：`1.0.0-debug.20260711.t130259.g06894bb`。

运行日志：`/home/ff/uav/tars/huida_uav/.devseek/runs/20260711-131537.log`。

Harness 报告：`/tmp/devseek-real-plugin-deepseek-tThOwG/report.json`（临时目录可能被系统清理，关键事实已固化在本文）。

产物：`/home/ff/uav/tars/huida_uav/src/oam/src/lifting/zc_maintenance/202607111312/docs/license-transport-facts.md`。

任务要求读取 `license_types.hpp`，提取六个常量并创建 Markdown。结果如下：

| 强事实 | 源码真实值 | DevSeek 产物值 | 结论 |
| --- | --- | --- | --- |
| `kTopicLicenseState` | `/uav/license/state` | `/uav/dt/license/state` | 错误 |
| `kTopicLicenseTunnelRx` | `/uav/license/tunnel/rx` | `/uav/dt/license/tunnel/rx` | 错误 |
| `kMavTunnelCmdLicense` | `33007` | `300` | 错误 |
| `kTunnelVersion` | `1` | `1` | 正确 |
| `kTunnelMaxTotalLen` | `64 * 1024`，即 `65536` | `81920` | 错误 |
| `kTunnelSessionTimeoutMs` | `5000` | `30000` | 错误 |

本次证明：

1. 混合动作路由和无损写盘已生效。
2. 文件存在、关键词存在、格式基本成立，仍不能证明任务完成。
3. 六项核心事实仅一项正确，说明“工具读取结果 -> 交付物事实”没有强约束。
4. `formal-project-document-quality` 的 license/Tunnel 专用规则污染了短 canary，引导模型补写无关接口、修改清单和臆造文件。
5. 运行没有形成 `agent-run-completed` 终态，Runtime Replay 未通过。
6. 当前提交的真实资格仍为 canary `0/3`、medium `0/2`、formal `0/1`，不得宣称稳定。

### 2.3 当前成熟度估计

该百分比是相对 Claude Code/Codex 的工程成熟度估计，不是测试覆盖率：

| 能力层 | 当前估计 | 主要缺口 |
| --- | ---: | --- |
| 状态、日志、失败保真、版本绑定 | 70% | 仍有无终态和分支结算 |
| 工具协议与写盘 | 60% | Web 文本工具仍弱于 native structured tools |
| 意图识别与任务形态 | 55% | 复杂约束作用域和跨轮意图仍需契约化 |
| 简单编程任务闭环 | 45% | 事实 grounding 和语义验证不足 |
| 正式项目调查、设计、实现、验证 | 25% 至 35% | 项目级证据、集成边界、成果质量不稳定 |
| 真实稳定性 | 未达标 | 当前无同提交真实成功配额 |

综合成熟度约 40% 至 45%。当前已经从“容易假成功”进步到“多数失败能够被看见”，但距离“稳定完成任务”仍有核心架构缺口。

## 3. Claude Code/Codex 对标基线

后续每类修改必须先写出同类对标，不得只写“参考 Codex”。至少回答：

1. **任务契约**：Codex/Claude 如何从用户请求提取目标、输入、输出、禁止项和验收条件？
2. **上下文发现**：如何从用户路径扩展到 repo root、构建入口、调用链、参考模块和项目规则，而不是困在提示词目录？
3. **工具事实**：模型只提出工具意图，宿主如何执行、保存原始结果并把它变成后续可引用证据？
4. **变更边界**：如何在既有项目中定位集成点、复用原接口并避免孤岛实现？
5. **验证闭环**：如何根据项目选择测试，如何处理不可运行、GUI、超时和人工验收？
6. **失败恢复**：模型截断、Provider 断线、旧进程、工具失败和续作如何保留事实、避免重复副作用？
7. **用户反馈**：如何用阶段总结说明进展，同时把文件读取和命令折叠成次级细节？

对标结果必须落到 DevSeek 的职责所有者、状态迁移、数据结构、测试和删除项。不得通过复制固定提示词模拟竞品能力。

## 4. 目标架构

### 4.1 TaskContract 是任务语义唯一来源

新增或收敛为一个 Provider 无关的 `TaskContract`，至少包含：

- `taskShape`：existing-project、standalone、inspection、documentation、verification、repair、resume、destructive。
- `objectives`：用户真正要求完成的结果。
- `inputs`：指定文件、目录、会话事实和用户提供资料。
- `deliverables`：文件、代码变更、文档、命令结果或只读结论，不固定数量。
- `constraints`：禁止修改的对象、允许写入的目录、语言、兼容和安全约束。
- `evidenceRequirements`：哪些结论必须由源码、命令、测试或用户确认支持。
- `verificationContract`：每个交付物的可执行验收条件。
- `completionPolicy`：什么情况下 delivered、failed、blocked 或 waiting_user。

意图分类器只负责构造或补充 `TaskContract`，不能直接决定“已完成”。计划、工具权限、QualityGate、Replay、UI 和恢复都消费同一个契约。

### 4.2 EvidenceRef 是事实唯一来源

所有工具结果归一成不可变 `EvidenceRef`：

- `evidenceId`、`operationId`、工具类型、工作区和运行版本。
- 源文件真实路径、行区间、内容哈希和读取时间。
- 命令、cwd、退出码、stdout/stderr 摘要和原始 payload 引用。
- 写盘前后哈希、diff、read-back 结果。
- 验证类型、状态、失败原因和是否需要人工确认。

模型可以总结 Evidence，但不能覆盖 Evidence。恢复、续作和最终回答都从 EvidenceStore 重建，不依赖网页聊天文本。

### 4.3 ArtifactClaim 连接证据和交付物

交付物中的可验证强事实必须转换成 `ArtifactClaim`：

- claim 内容和规范化值。
- 来源 `EvidenceRef`。
- 目标文件和位置。
- validator 类型，例如 exact、numeric、path-exists、symbol-definition、command-exit、schema。
- 验证状态和差异。

如果任务明确要求提取六个常量，六项都必须建立 claim 并与源码一致。只检查 `kMavTunnelCmdLicense` 关键词存在属于弱 oracle，必须禁止。

### 4.4 QualityPolicy 由任务契约组合

删除“所有正式项目都需要协议数值、遥控器接口和修改清单”这类领域硬编码。由 `TaskContract` 组合通用策略：

- 文档任务检查标题、结构、请求覆盖、事实 claim、路径和 read-back。
- 既有项目功能检查 repo 调查、集成点、修改范围、构建和回归。
- 协议任务仅在用户请求或代码证据表明存在协议时检查 schema、request/response、数值和兼容。
- 纯常量报告不生成实现方案、接口文档或修改清单。
- 独立项目不强制引用不存在的主控链路。

迁移完成后直接删除被新策略替代的领域专用判断和兼容分支，不同时保留两套完成判定。

### 4.5 单一 Headless Agent Core

统一状态至少覆盖：

```text
needs_context
  -> tool_requested
  -> tool_executed
  -> evidence_collected
  -> change_prepared
  -> change_applied
  -> verified
  -> delivered

任意阶段 -> failed / blocked / waiting_user / manual_review_required
```

规则：

1. Analyze/Edit/Run、自由 Agent、Markdown 快路径、恢复和历史续作不能自己结算。
2. 失败是粘性的，只有新的有效证据和显式恢复迁移可以解除。
3. 每轮必须取得新 Evidence、状态推进或明确终态，否则计为 no-progress。
4. 达到恢复预算、相同工具重复预算或相同 Provider 输出预算后直接失败，不能无限“继续生成”。
5. VS Code、CLI 和未来 API 入口只做 Surface Adapter。

### 4.6 Provider 和 UI 边界

- DeepSeek Web、DeepSeek API、OpenAI-compatible API 和其他模型只实现 Provider Adapter。
- native function calling 优先使用结构化 schema；Web 文本工具协议仅是兼容层。
- Provider 输出完整性必须分类为 complete、tool-call、short-intent、truncated、error-page、auth-failed、empty。
- UI 只渲染 append-only 的领域事件，不覆盖前一条阶段总结。
- 用户可见总结说明“完成什么、发现什么、下一步什么”；文件和命令折叠展示，不重复刷屏。

## 5. 最快收敛路线

### P0-A：Evidence Grounding 垂直闭环

目标：先让一个最小真实任务做到 100% 事实正确，再扩展任务规模。

实施：

1. 将 `20260711-131537` 固化为 replay fixture，输入包含真实 `license_types.hpp`，oracle 精确检查六个值。
2. 建立 `EvidenceRef -> ArtifactClaim -> VerificationResult` 最小垂直链。
3. `read_file` 的原始内容和哈希进入 EvidenceStore；模型生成报告后由宿主重新读取源码和报告进行 claim 比对。
4. 任一 claim 缺失或不一致时禁止 `task_complete`，反馈具体差异，允许一次有界修复。
5. 修复后再次 read-back；只有六项全部正确、格式门禁通过、终态存在、Replay 通过才算成功。

退出条件：fixture、仿真和一次真实 canary 全部通过，且报告没有任务外臆造内容。

### P0-B：通用 TaskContract 与 QualityPolicy

目标：消除当前维保/license 测试对所有任务的污染。

实施：

1. 审计并重构 `formal-project-document-quality.ts`、`formal-project-source-quality.ts`、`markdown-deliverable-task.ts` 和 harness 中的领域规则。
2. 把源码事实、协议、接口、修改清单、代码和验证要求改为可组合 obligation。
3. `artifact-root` 只提供时间戳、`docs/src` 目录和用户指定交付物，不追加固定维保方案。
4. 增加无关领域测试，例如普通配置提取、TypeScript bugfix、独立 Python 工具，证明没有 license/Tunnel 关键词依赖。
5. 新核心接管后删除旧规则，不为兼容保留双轨 QualityGate。

退出条件：同一质量核心可正确处理纯报告、既有工程功能、独立项目和协议任务，且各自只要求相关证据。

阶段记录（2026-07-11，第一切片）：

- 新增 Provider 无关的 `agent/task-contract.ts`，统一提取 task shape、输入、交付物、约束与可组合 quality obligation。
- `formal-project-document-quality` 已改为消费 TaskContract；普通配置事实提取、普通分析报告、TypeScript bugfix、独立 Python 工具不再继承 license/Tunnel、遥控器接口或既有工程修改清单要求。
- 协议、接口、通信链路、既有工程修改与验证只在请求明确涉及对应语义时组合。
- 新增四类非 license/Tunnel 隔离测试，并保持正式协议项目与 Markdown 主/回退链路回归通过。
- 本切片尚未完成 P0-A `EvidenceRef -> ArtifactClaim -> VerificationResult`，不得将其记为 P0-A 完成或真实资格提升。

### P0-C：运行终态与有界恢复

目标：消除“文件已写但没有 terminal event”、死循环和继续生成反复。

实施：

1. 所有 run 由 `RunContext` 创建并由统一 settlement service 结束。
2. 设置工具、Provider continuation、相同响应和 no-progress 的独立预算。
3. 任何退出路径都写入 `agent-run-completed`、`agent-run-failed`、`agent-run-blocked` 或 `agent-run-waiting-user`。
4. Harness 超时不能只杀进程，必须保留最后稳定 checkpoint 和明确 terminal reason。
5. Replay 断言每个 run 恰有一个最终终态，且终态与 Todo、QualityGate、ReviewLedger、UI 一致。

退出条件：历史无终态、Provider 断线、截断、旧 Bridge、重复继续和工具失败 fixtures 全部通过。

### P1-A：项目级调查与规划

目标：达到 Codex/Claude 在既有大项目中的上下文发现能力。

实施：

1. 从用户路径向上定位 repo root、AGENTS/CLAUDE、构建文件、模块清单和工作区边界。
2. 根据用户提到的参考模块搜索调用者、接口实现、注册入口、线程、传输和构建集成点。
3. 计划必须覆盖目标、依赖、修改文件、验证和风险，但任务简单时保持简洁。
4. 设计文档按任务需要生成，不固定两个 Markdown，也不把文档生成当成代码完成。
5. 既有项目实现必须说明与主流程的关系，独立任务不得强行寻找主控链路。

### P1-B：代码实现与验证规划

目标：从“模型给代码”升级为宿主可验证的软件工程变更。

实施：

1. 变更前建立目标文件、符号、调用关系和项目规则证据。
2. 由 WorkspaceEdit/ChangeSet 统一写盘，保留 diff、checkpoint 和幂等 operationId。
3. 根据项目自动选择最小有效验证，再扩展到模块测试和项目构建。
4. 编译失败进入 repair contract；GUI/交互式验证进入人工验收，不伪装 exit 0。
5. 最终交付列出文件、关键改动、验证命令、结果和未验证风险。

### P1-C：用户体验和进度事件

目标：像 Codex 一样让用户理解工作，而不是看到重复文件名。

实施：

1. 阶段总结 append-only，后续消息不能覆盖。
2. 每个阶段最多一条用户可见总结，说明结果和下一步。
3. 读取、搜索、命令和工具参数进入可折叠 activity group，并去重相同操作。
4. Todo 展示稳定任务语义，不随文件或轮次重复创建。
5. failed、blocked、waiting_user、manual_review_required 使用不同语义和操作入口。

### P2：架构债务收敛和 Provider 扩展

目标：让能力增长不再依赖超大文件和 DeepSeek Web 特例。

实施：

1. 按架构预算从 `extension.ts`、`agent-loop.ts`、`agentic-loop.ts`、`tool-loop.ts`、`fake-tool-parser.ts`、`deepseek-agent.ts`、`webview.js` 外移职责。
2. 每次抽取后下调预算，不能回升。
3. `fake-tool-parser` 只做 Web compatibility parsing；业务事实和完成判断全部迁出。
4. Provider contract tests 覆盖 Web text tool、native function calling、API fallback 和恢复语义。
5. 在有可用账号/配置时增加第二 Provider 的真实小任务，但不把 Provider 差异写入 Agent Core。

## 6. 每轮固定迭代算法

每轮只处理一个失败类别：

1. 读取当前 HEAD、05、本文和最近真实报告。
2. 用 Claude Code/Codex 行为写出对标差距和职责所有者。
3. 只复现一次，保存完整日志、成果物和版本。
4. 把失败转成最小 replay，先看到测试失败。
5. 审计同类入口、状态流、协议、恢复和 UI，确定架构修复范围。
6. 修改拥有该事实的核心边界，删除被替代旧逻辑。
7. 运行 focused unit 和 replay。
8. 运行 Extension/Bridge/PA0-PA13/architecture 完整 benchmark。
9. benchmark 通过后提交一个可回滚 commit。
10. 构建精确 commit VSIX，验证 packaged Bridge，安装精确 VSIX。
11. 先跑一个可见短 canary，审计运行日志、Replay、实际产物和 UI。
12. canary 通过才进入 medium/formal；失败则回到第 3 步。

停止规则：

- 同一失败类别连续出现两次，禁止第三次盲跑，必须重新审计架构和 oracle。
- 没有新 Evidence、状态推进或新 fixture 时，不继续让 Provider“再试一次”。
- 用户要求暂停时停止 harness、VS Code 测试窗口和 Bridge 后台进程。
- 确定性门禁通过只表示 regression-safe，不表示真实稳定。

## 7. 测试阶梯

### L0：静态和架构门禁

- TypeScript 编译、lint/diff check、架构预算、边界守卫。
- 禁止新增领域硬编码、重复完成判断和 Surface 业务逻辑。

### L1：Unit

- TaskContract 约束作用域。
- EvidenceRef 不可变与 claim validator。
- 状态迁移、失败粘性、恢复预算。
- Provider 完整性和 ToolCall 归一化。
- QualityPolicy obligation 组合。

### L2：Runtime Replay

必须永久覆盖：

- 工具意图未执行。
- 工具执行后短答。
- Provider 断线、截断、空响应、错误页和登录失败。
- 旧 Bridge/旧 VSIX 接管。
- 重复 continuation 和同一路径重复写入。
- 缺少结论、无终态和 UI 假完成。
- 源码事实与产物不一致。
- “只读源码 + 写新交付物”约束作用域。
- GUI/长运行等待人工确认。

### L3：Headless/仿真

- 使用真实 Agent Core、真实工具和临时工作区。
- Provider 可使用确定性 fixture，但不能绕过状态机。
- 断言文件内容、diff、命令、终态、Replay 和用户事件。

### L4：真实 VS Code + DeepSeek Web

- 使用精确 VSIX、可见浏览器、已登录 cookie 和真实工作区。
- 每次测试使用 `YYYYMMDDHHmm/docs`、`YYYYMMDDHHmm/src` 独立目录。
- 真实任务 prompt 不注入答案或领域固定模板。
- Harness assertion 必须检查任务特定事实，不只检查关键词和文件大小。

### L5：正式项目与竞品对标

- DevSeek 和 Codex/Claude 接收相同用户请求和相同初始工作区。
- 成果物分别保存在带时间戳的独立目录，Codex 目录可以包含 `codex` 标识。
- 由统一 rubric 盲审需求覆盖、项目证据、设计、代码、验证、风险和用户反馈。
- 不把 Codex 产物放入 DevSeek prompt，避免答案泄漏。

## 8. 资格门禁和评分

### 8.1 稳定资格

沿用 05 的最低稳定门禁。同一干净提交必须连续通过：

- 3 个 short canary。
- 2 个 medium integration task。
- 1 个 formal existing-project task。
- 所有报告 Replay 成功、版本匹配、无 P0/P1 错误。

### 8.2 优秀资格

“稳定”不等于“优秀”。优秀资格还必须在同一发布候选系列完成：

- 至少 10 个短任务，覆盖只读、写文档、单文件修改、命令验证和混合约束。
- 至少 6 个中型任务，覆盖不少于 3 种语言或项目形态。
- 至少 3 个正式项目任务，覆盖调查、设计、实现、测试和成果物审计。
- 至少 5 个故障恢复任务，覆盖 Provider、工具、版本、续作和人工验收。
- 至少 2 个与维保/license 无关的项目，证明通用性。
- 0 次假成功、0 个未声明的错误事实、0 个重复副作用、0 个无终态运行。

统一 100 分 rubric：

| 维度 | 分值 | 硬失败条件 |
| --- | ---: | --- |
| 意图和约束 | 10 | 任务类型或禁止项作用域错误 |
| 项目调查和证据 | 15 | 强事实无来源或关键入口遗漏 |
| 设计与集成边界 | 15 | 孤岛设计、与既有主流程脱节 |
| 代码正确性与范围 | 20 | 不编译、越界修改、明显缺陷 |
| 验证与恢复 | 20 | 测试未执行却宣称通过、重复副作用 |
| 交付物事实质量 | 10 | 臆造数值、路径、接口或验证结果 |
| 用户反馈与可追溯 | 10 | UI 假完成、终态和日志不一致 |

优秀资格要求平均分不低于 90，任一正式任务不低于 85，且没有任何硬失败。DevSeek 与 Codex/Claude 的同题结果逐项比较，差异必须转化为规则、fixture 或架构任务，不能只写主观点评。

## 9. 新窗口前三轮执行顺序

### 第一轮：修复事实闭环，不跑正式任务

1. 固化 `20260711-131537` replay 和六项 exact oracle。
2. 完成最小 `TaskContract`、`EvidenceRef`、`ArtifactClaim` 垂直链。
3. 把纯常量报告从正式项目专用门禁中解耦。
4. focused tests 和完整 benchmark 通过后提交。
5. 打包、安装精确 VSIX，只跑一次相同 canary。

### 第二轮：证明通用，不继续打磨同一道题

在第一轮 canary 成功后，使用一个完全无关的小任务，例如读取 TypeScript 配置事实并生成报告，或修复一个独立测试 fixture。检查：

- 没有 license/Tunnel 固定要求。
- TaskContract 正确选择 obligation。
- 交付物事实逐项可追溯。
- UI 阶段总结不重复、不覆盖。

### 第三轮：进入一个 medium 编程任务

选择真实但范围受控的现有项目 bugfix：要求调查调用链、修改代码、运行测试、失败时修复并交付。与 Codex 做同题对标，第一次完整评估设计、代码和验证能力。

只有前三轮都通过，才恢复维修保养正式项目的全流程测试。

## 10. 提交、发布和文档规则

1. 每个独立失败类别一个 commit，提交信息说明能力边界，不写截图编号。
2. 提交前必须通过相关 focused tests 和完整 benchmark。
3. Extension/Bridge 行为变更后必须 compile、package、验证 packaged Bridge、安装精确 VSIX。
4. 真实测试报告必须记录 Git commit、VSIX build、workspace、scenario、artifact 和 Replay。
5. 每轮同步 05、TOP_AGENT_CHANGE_GATE、Changelog 和对应专题设计。
6. 迁移完成后删除旧实现、死代码、重复 parser 和兼容分支；删除前用调用搜索、测试和运行证据确认功能已迁移。
7. 不提交真实业务项目生成物到 DevSeek repo；测试 fixture 使用脱敏最小样例。

## 11. 新窗口每轮汇报模板

每轮结束只报告可验证事实：

```text
当前提交：
本轮失败类别：
Claude Code/Codex 对标差距：
架构根因和职责所有者：
新增 replay：
代码删除/新增：
确定性测试：
精确 VSIX：
真实任务与成果物质量：
稳定资格：canary x/3, medium x/2, formal x/1
剩余最高优先级：
是否允许进入下一层：是/否，证据是什么
```

## 12. 接管红线

- 不得把文件存在、Todo 变绿、Provider 长回答或模型自称完成当作成功。
- 不得为了通过当前维修任务写入 license、Tunnel、遥控器、固定文档名或固定文档数量的通用规则。
- 不得在 Surface、parser 或 Provider Adapter 中加入业务完成判断。
- 不得连续重跑相同真实失败而没有新增 fixture 和架构变化。
- 不得用单元测试替代真实 VS Code 插件证据。
- 不得宣称稳定，直到同提交真实配额和成果物审计全部通过。
- 不得为了兼容已被证明不合理的新开发代码而保留双轨实现。

最快达到优秀能力的路线不是继续扩大提示词，而是先让任务契约、工具证据、产物事实、验证和终态成为宿主控制的确定性闭环。模型负责推理和候选方案，DevSeek Runtime 负责事实、权限、执行、恢复和交付真相。
