{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://devseek.local/schemas/devseek-run-evidence-receipt-v1.json",
  "title": "DevSeek Product Run Evidence Receipt v1",
  "$comment": "Product diagnostics only. receipt_sha256 is the canonical SHA-256 of this object without receipt_sha256.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "protocol",
    "integrity_scope",
    "qualification_eligible",
    "run_id",
    "sequence",
    "event_sha256",
    "previous_event_sha256",
    "idempotency_key",
    "committed_at",
    "receipt_sha256"
  ],
  "properties": {
    "protocol": { "const": "devseek.run-evidence-receipt/v1" },
    "integrity_scope": { "const": "product-run-diagnostics" },
    "qualification_eligible": { "const": false },
    "run_id": { "$ref": "#/$defs/text512" },
    "sequence": { "type": "integer", "minimum": 1, "maximum": 9007199254740991 },
    "event_sha256": { "$ref": "#/$defs/sha256" },
    "previous_event_sha256": { "$ref": "#/$defs/nullableSha256" },
    "idempotency_key": { "$ref": "#/$defs/text512" },
    "committed_at": { "$ref": "#/$defs/canonicalTime" },
    "receipt_sha256": { "$ref": "#/$defs/sha256" }
  },
  "$defs": {
    "text512": {
      "type": "string",
      "minLength": 1,
      "maxLength": 512,
      "allOf": [
        { "pattern": "^(?!\\s)(?![\\s\\S]*\\s$)(?![\\s\\S]*[\\u0000-\\u001F\\u007F-\\u009F])[\\s\\S]+$" },
        { "pattern": "^(?![\\s\\S]*devseek-ra1_[A-Za-z0-9_-]{43})[\\s\\S]*$" }
      ]
    },
    "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "nullableSha256": {
      "oneOf": [
        { "$ref": "#/$defs/sha256" },
        { "type": "null" }
      ]
    },
    "canonicalTime": {
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-5][0-9]\\.[0-9]{3}Z$"
    }
  }
}
