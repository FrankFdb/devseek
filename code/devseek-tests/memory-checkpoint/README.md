# Memory And Checkpoint

This fixture preserves the original I10 user inputs for canonical memory authority
and interruption recovery. `scenario.json` is the durable input record. The linked
automated tests execute the product boundaries; `runs/` contains their unedited TAP
output and summary receipts.

The five permanent coding fixtures are intentionally absent. They run separately as
the regression baseline and cannot count as I10 evidence.
