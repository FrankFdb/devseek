{
  "name": "@devseek-netai/cli",
  "version": "2.0.11",
  "private": true,
  "main": "dist/index.js",
  "bin": {
    "devseek": "dist/index.js"
  },
  "scripts": {
    "build": "npx esbuild src/index.ts --bundle --platform=node --format=cjs --external:readline/promises --banner:js='#!/usr/bin/env node' --outfile=dist/index.js",
    "typecheck": "tsc --noEmit --pretty false",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@devseek-netai/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.4.5"
  }
}
